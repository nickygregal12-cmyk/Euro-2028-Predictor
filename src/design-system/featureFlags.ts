// Design-system feature flags. Kept tiny and explicit; no config lib.

export const FEATURES = {
  // Whole-card navigation to the match centre (Tier 4). In v0.1 the chevron
  // and navigation are simply not rendered — same MatchCard component.
  matchCardNavigation: false,
}
