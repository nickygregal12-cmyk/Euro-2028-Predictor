# Nightly run — 5 August 2026, 04:59 session

Unattended overnight session against `nickygregal12-cmyk/Euro-2028-Predictor`. British English throughout, per instructions.

**Note on filename:** originally written to `claude/nightly/2026-08-05-nightly-run.md`. A second, independent scheduled session (`claude/nifty-mendel-u302sn`, PR #465) was writing a different report to the identical path at the same time. Mine merged into `main` first (via PR #466); renamed here to `2026-08-05-0459-nightly-run.md`, after the session's approximate start time, to match the timestamped convention `docs/automation-runs/` already uses and stop the two colliding. See §6.

## 1. What I inspected

Sources of truth, in the order specified:

- **GitHub `main`**: at session start, `main` was at `653511a` ("Record the 104 and 105 development rollout with corroborating evidence", #464). Two open PRs existed: #465 (a docs-only finding from an earlier scheduled run, awaiting human review) and #462 (a docs-only automation handover record, green, base slightly behind `main`).
- **Repo docs**: `AGENTS.md`, `CLAUDE.md`, `docs/quality/current-status.md`, `docs/roadmap.md`, `MASTER-TODO.md`, `docs/quality/risk-register.md`.
- **Existing `claude/…` automation history**: `docs/automation-runs/2026-08-04-0100-*`, `2026-08-04-0300-*`, `2026-08-05-0100-*`.
- **Code and tests**: full repository build, lint, typecheck and unit-test suite; targeted reading of `src/features/trends/`, `src/services/supabase/predictionConsensus*.ts`, `src/features/home/` (the established "unavailable vs empty" pattern), and the risk register's open Low/Medium items, cross-checked against actual code rather than trusted at face value.
- **Deployed app**: not touched. No Netlify or Supabase inspection performed — the chosen batch was application-code-only.

**Environment constraint, verified directly:** this sandbox has no Docker daemon at all (`docker pull` fails with "no such file or directory" on `/var/run/docker.sock`), so `supabase start`, pgTAP and Database parity cannot be run here. Any SQL/migration/RPC/RLS work is therefore unverifiable in this environment and was correctly ruled out as a candidate batch, per the "fail closed" instruction.

**This ran much longer than a normal session, and the repository moved substantially underneath it.** Between opening PR #466 (04:59) and it finally merging (~10:2x), `main` advanced from contract 105 through contract 109 (LMS restart driver, its calendar guard and its successor-window scheduler — ADR 0025 decision 1 completing in full) plus a documentation-reconciliation series, all landed by other concurrent/scheduled sessions. §6 covers what that motion did to this session's own findings.

## 2. Housekeeping done before selecting new work

Two pre-existing open PRs needed a decision before starting fresh work:

- **PR #462** ("Docs: record 2026-08-05 03:00 progress handover") — a pure documentation addition matching the repository's established dated-handover pattern, CI green. **Merged** (squash).
- **PR #465** — docs-only and CI-green, but deliberately left open by its own author because it documented a live scoring-authority finding (`DATA-009`, see §6). Left untouched at the time; revisited below once its status changed materially.

## 3. The batch I chose and why

**Batch: distinguish a failed player-name read from a genuinely player-less pick on the Prediction Trends page (risk-register item `UX-002`).**

Selection reasoning, against the stated priority order:

- (a) **Scoring/data-integrity/auth/admin defects**: the one live candidate (`DATA-009`) was already reported and correctly held by PR #465 — attempting an unverified fix to scoring-authoritative SQL with no way to run pgTAP would be exactly what the "fail closed" instruction rules out.
- (b) **Unblocking in-progress work**: `MASTER-TODO.md`'s Stage E/F "next slice" markers (recurring matchweek scheduler, LMS settlement job) turned out to be **stale** — both were already landed per `docs/quality/current-status.md` and the actual migrations. No genuine application-code gap to unblock there.
- (c) **Highest-value active roadmap item**: the next real roadmap items (Cup split-stage persistence, provider-ingestion custody) are schema/SQL-shaped and out of reach in this sandbox.
- (d) **Hardening**: this is where the batch landed. A research agent surveyed the risk register's open Low/Medium items against actual code. Most candidates were already resolved-but-undocumented (`SEO-001`, `TYPE-001`) or too large/risky for one unattended session without integration coverage (`CODE-001`'s 892-line `PredictionsProvider.tsx`). `UX-002` on `PredictionTrendsPage.tsx` was live, small, self-contained, and had an established, well-tested fix pattern already proven elsewhere (`useHomeData.ts`'s `unavailable` set, `HomePage`'s warning banner, `MatchCentrePage`'s league-scope handling).

**The defect, concretely:** `PredictionTrendsPage.tsx` fetched Golden Boot player names with `fetchConsensusPlayers(...).catch(() => [])`. Every label reading from the result already has a fallback (`?? 'Official squad player'`), so a genuine read *failure* (network blip, RLS hiccup) rendered identically to a pick that legitimately has no matching player row.

## 4. Exactly what changed

**Branch:** `claude/prediction-trends-player-name-availability`
**PR:** [#466](https://github.com/nickygregal12-cmyk/Euro-2028-Predictor/pull/466) — **merged** to `main` as `170636e`.

Files:

- `src/features/trends/PredictionTrendsPage.tsx` — the player-name fetch now tracks its own failure (`playersUnavailable: boolean`) separately from the consensus fetch's own error handling. When it fails **and** there are Golden Boot picks that would actually be mislabelled, a `warning`-variant `Alert` ("Player names are temporarily unavailable") renders above the trends content, matching `HomePage`'s existing pattern. A failure with zero Golden Boot picks stays silent.
- `tests/features/trends/PredictionTrendsPage.test.tsx` — new file, four cases: failed read shows the warning + existing fallback label; successful read shows real names with no warning; no warning when there are no Golden Boot picks; the suppressed-consensus path never calls `fetchConsensusPlayers` at all.
- `docs/quality/risk-register.md` — recorded this partial `UX-002` closure.

No SQL, RPC, RLS policy, migration, scoring, or lock logic touched. No database object changed. No production, Supabase or Netlify state touched or inspected.

## 5. What I tested and the results

All green on the final branch head before merge, rebased onto latest `main` (twice, as `main` kept moving):

| Check | Result |
| --- | --- |
| `npx oxlint --deny-warnings` | Pass |
| `npx tsc -b` | Pass |
| `./node_modules/.bin/vitest run` (full suite) | **2303 passed, 26 skipped, 0 failed** — the 26 skips are pre-existing DB-gated/git-worktree-gated suites, not a new gap |
| `tests/scripts/riskRegisterIntegrity.test.ts`, `ciTestDiscoveryFloor.test.ts`, `documentationContractFreshness.test.ts` | Pass |
| `npm run build` | Pass |
| `npm audit --omit=dev --audit-level=high` | 0 vulnerabilities |
| Database parity / pgTAP | Not applicable — no SQL touched, and no Docker daemon in this sandbox regardless |
| Browser E2E (Playwright, via CI's `authenticated-browser` job) | **Ran in CI and passed** before merge |
| CI (GitHub Actions `ci`) | **Passed** before merge |

## 6. Merge outcome, and what changed after the report was first written

**PR #466: merged.** Low-risk, application-only (UI + test + docs), full local verification passing, CI green — merged per the stated auto-merge policy once checks completed. This took far longer than expected (PR opened 04:59, merged after 10:2x) purely because of GitHub Actions queue/run time, re-triggered twice by unrelated pushes (a `main`-merge to keep the branch current, and this report itself).

**PR #462: merged** (squash) — see §2.

**PR #465 / `DATA-009`: already resolved on `main`, independently, by a different concurrent session — before I could act on it.** While waiting for #466's CI, `main` advanced through contracts 106–109. I checked what those contracts actually did (not just their commit-message titles), because contract 107 is described as "the LMS restart driver" — exactly the future writer PR #465's investigation named as the thing that would make `DATA-009` reachable. Findings, verified by reading the committed SQL directly:

- **Contract 106** (`20260805020000_terminal_aware_bonus_rederive.sql`, PR #467) fixed the actual defect: `recompute_ko_predictor_for_match` and `recompute_lms_for_tournament` now resolve through `predictor_internal.current_public_competition_id` (which falls back to the latest completed instance) instead of `predictor_internal.live_competition_id` (which excludes it) — exactly remedy 1 from PR #465's own investigation. Proven behaviourally by `supabase/tests/157_terminal_aware_bonus_rederive.sql`: completing a KO Predictor competition, correcting its result, and requiring both entrants' stored scores to move.
- **Contract 107** (`20260805030000_lms_restart_driver.sql`) then landed `predictor_internal.restart_lms_competition`, which does write `completed_at` on a `last_man_standing` `bonus_competitions` row — confirming contract 106 was landed deliberately *before* this driver, specifically so the driver would not ship a live scoring defect.
- A comment left on PR #465 by another session (posted 08:12, under the repository owner's account, which these automated sessions post through) independently verified the same thing against the committed SQL and confirms this reading.
- PR #465's own branch has since been rebased forward (commit `835f761`, "Merge main; close DATA-009 and correct a row three contracts overtaken") to record `DATA-009` as **resolved at contract 106** rather than open, and to correct `docs/quality/current-status.md`'s "Next executable issue" row, which had gone stale a second time (it still named the LMS restart driver as pending, when contracts 107–109 had by then finished it).

**Net effect: nothing further is needed from me or from you on `DATA-009` — it is fixed and evidenced on `main`.** PR #465 itself is still open only because of the filename collision below; its substantive content is accurate and ready. I did not merge PR #465 myself: it is not my branch, and while I independently confirmed its technical claims, the decision to land another session's PR sits with whoever is driving that branch (or with you) rather than with this session.

**One collision this discovery surfaced:** PR #465 also adds a file at `claude/nightly/2026-08-05-nightly-run.md`, the same path this report originally used, with completely different content (a different session's separate report from 03:28). My version reached `main` first via #466. If PR #465 merges as-is now, GitHub will show an add/add conflict at that path. I've resolved my side by renaming this file to include the session start time (see the note at the top) — `main` no longer holds anything at the original colliding path, so PR #465 should now be able to merge cleanly without anyone needing to touch its content. I did not rename or edit anything on PR #465's own branch.

## 7. What remains uncertain / decisions needed from you

1. ~~`DATA-009`~~ — **resolved**, see §6. No action needed.
2. **Merge PR #465 when convenient.** Its content is accurate, CI was green as of its last commit, and the filename collision that was the only structural blocker is now cleared from the `main` side. Ordinary docs-only low-risk merge.
3. **This sandbox's DB access is a standing constraint**, not just tonight's: any future batch needing a migration, an RPC, an RLS change, or pgTAP/Database-parity verification cannot be safely completed unattended here. Worth deciding whether future scheduled runs should be pointed at an environment with Docker/local-Supabase access, or kept scoped to application-code-only batches as tonight's was.
4. **Two (at least) independent automated sessions worked this repository concurrently tonight**, as PR #465's own review comment also noted. Tonight it produced real but recoverable churn: a stale "Next executable issue" row corrected twice, and this filename collision. Worth deciding whether that's an acceptable cost of running scheduled sessions this frequently, or whether some light coordination (a claimed-contract-number ledger, or a timestamped-path convention for *all* dated automation output, not just `docs/automation-runs/`) is worth adding.
5. `MASTER-TODO.md`'s Stage E/F status lines were stale against `docs/quality/current-status.md` and the actual migrations at session start (both "next slice" items were in fact already landed). Not edited by me — flagging rather than folding into an unrelated PR.

## 8. Recommended next batch

In priority order:

1. **Merge PR #465** (housekeeping, see §7.2) — no engineering decision required, the substance is already settled.
2. **The Predictor Championship phase driver**, per `current-status.md`'s now-current "Next executable issue" row: rank the initial table, create the two child groups, assign entrants and schedule the immutable split fixtures (ADR 0025 decision 2). It has a named prerequisite — nothing in the repository creates windows for a season competition, and `bonus_cup_fixtures.window_id` is `NOT NULL` — so a season round calendar needs to land first. This is squarely schema/SQL work and needs an environment with real Postgres access.
3. **Reconcile `MASTER-TODO.md`'s Stage E/F entries** against `docs/quality/current-status.md` — small, docs-only, safe for an unattended session.
4. If another DB-free application batch is wanted: `src/domain/tournament/homeDashboard.ts`'s `catchUpSummary()` hard-codes `rankDelta: null` even though `rank_history` storage already exists and is populated — a real, medium-sized, fully vitest-verifiable feature-completion candidate. Left for a future run to keep this PR to one coherent concern.
5. A quick, very low-risk win once a DB-adjacent environment is available: add `tests/database-parity/rateLimitParity.test.ts` asserting the `RATE_LIMITS`/`RATE_LIMIT_WINDOW_MS` constants in `src/domain/rateLimit.ts` against the literals in `supabase/migrations/20260720210000_rate_limits.sql` — a **text-based** parity test needing no live database, discharging the one concrete follow-up named in `docs/quality/investigations/2026-07-30-hygiene-002-module-reachability.md`.
