# Offline analytical layer (ai/analytics).
#
# Separate from requirements.txt on purpose: the training and prediction
# pipeline does not need DuckDB, and the research layer should not be able to
# make the core package heavier or harder to install.
#
# DuckDB is an embedded analytical engine over local Parquet/CSV snapshots. It
# is never the application database — Supabase/Postgres stays the production
# authority — and nothing in ai/analytics opens a Postgres connection.
duckdb==1.5.5
