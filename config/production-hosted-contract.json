{
  "projectRef": "vkfnsqdyhvtwyqkisxhk",
  "requiredMigrationCount": 218,
  "latestMigrationVersion": "20260824100000",
  "latestMigrationName": "live_results_channel",
  "verifiedAt": "2026-08-24T13:33:29.656Z",
  "evidence": {
    "source": "Contracts 212 through 218 crossed into Production as ONE boundary on 24 August 2026 by guarded rollout run 32732830493 from exact main 9d302c8bc763ea62e5b630bdff4c4ef429e4930a, gated on fresh encrypted backup 32722811725 and on exact-head rehearsal 32731886075, which dumped Production, restored it into a disposable local Supabase and applied the boundary to THAT COPY ONLY. Development was moved first and Production second, the order the workflows refuse to invert. THIS RECORD IS WRITTEN BY HAND, and the reason is the one the contract-211 record already gives: a record pull request authored by github-actions[bot] does not trigger this repository's CI, so its merge gate never runs, and a superseded automation record drags the committed number BACKWARDS while the database sits ahead — and the promotion gate reads the record, not the database. EVERYTHING BELOW IS AN INDEPENDENT READ-ONLY QUERY AGAINST PRODUCTION taken at 2026-08-24T13:33:29.656264+00, separately from the rollout's own postflight. THE LEDGER HOLDS 218 ROWS AND NAMES 20260824100000 live_results_channel. EACH CONTRACT IS DRIVEN RATHER THAN READ OFF THE LEDGER, which only proves a file ran. Contract 212: get_season_matchweek_card(uuid,integer) reads season_prediction_lock_at and publishes both lock_at and locked. Contract 213: SportMonks tokens 14 through 21 are absent and every one resolves unknown, while measured token 10 still resolves postponed. Contract 214: predictor_internal.season_card_confirmation_reference is present. Contract 215: ai.current_fixture_recommendations now reads through ai.canonical_fixture_predictions, so a decision is current only when it belongs to the fixture's canonical forecast. Contract 216: predictor_internal.reminder_dispatch_runs exists with row level security on, and cron job player-reminder-dispatch is scheduled '*/5 * * * *' and active — 11 cron jobs before, 12 after, EXACTLY ONE ADDED. Contract 217: public.push_subscriptions exists with row level security on, 2 policies, ZERO anon or PUBLIC table grants and exactly delete,select for authenticated; reminder_deliveries.channel defaults to 'email'::text. Contract 218: public.matches is on the supabase_realtime publication, THE PUBLICATION CARRIES THAT TABLE AND NOTHING ELSE, and matches keeps replica identity 'd' — ADR 0008 accepted a narrow live-results channel and rejected broad user-owned or scoring tables, so a publication that had grown a second table would be the finding rather than a detail. THE GRANTS ON THE DROPPED AND RECREATED FUNCTION ARE THE SPECIFIC RISK CONTRACT 217 INTRODUCES AND THEY ARE INTACT: a dropped function takes its grants with it, and claim_due_reminders(integer, boolean) is executable by service_role and by NEITHER authenticated NOR anon. The once-per-action key is untouched at UNIQUE (user_id, action_key, reminder_kind), so a player with both channels available is still told once. THE SENDER ARRIVED INERT: reminder_sender_configuration() reports secrets_present false and configured false with no error, so the newly scheduled job records a refusal and posts nothing. NOTHING WAS SENT AND THIS PROMOTION SPENT NO PROVIDER CREDIT. NOT ONE PLAYER-OWNED ROW MOVED, every figure matching the contract-211 record: 1 auth user, 1 profile, 3 entries, 16 season predictions, 36 match predictions, 1 league, 1 league member, 578 season fixtures, 51 matches, 0 reminder deliveries and 0 push subscriptions. BOTH PUBLICATION GATES REMAIN FALSE and EURO PUBLICATION REMAINS HIDDEN. Two figures moved and neither is this boundary: ai.bets from 230 to 277, written by the AI Lab on its own schedule, and the fixture histogram from played 12, scheduled 566 to played 21, postponed 5, scheduled 552, written by provider ingestion across the four days since the contract-211 record. The histogram is compared WHOLE rather than by total for exactly this reason — a migration that invented a postponement would leave season_fixtures unchanged at 578 while moving a fixture between statuses, and this movement is accompanied by the provider evidence that produced it.",
    "workflowRunId": 32732830493,
    "rehearsalRunId": 32731886075,
    "backupRunId": 32722811725,
    "repositoryHead": "9d302c8bc763ea62e5b630bdff4c4ef429e4930a",
    "verificationSource": "independent Supabase read-only query against vkfnsqdyhvtwyqkisxhk after the successful guarded rollout, not that run's own postflight output"
  },
  "promotionAuthorised": false
}
