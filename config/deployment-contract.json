{
  "contractVersion": 53,
  "requiredMigrationCount": 53,
  "requiredRpcSignatures": [
    "public.replace_predicted_progression(uuid,jsonb,jsonb)",
    "public.delete_match_prediction(uuid,uuid,integer)",
    "public.admin_confirm_match_result(uuid,text,smallint,smallint,smallint,smallint,smallint,smallint,text)",
    "public.admin_correct_match_result(uuid,text,smallint,smallint,smallint,smallint,smallint,smallint,text)",
    "public.admin_clear_match_result(uuid,text)",
    "public.admin_match_result_revisions(uuid)",
    "public.admin_actual_third_place_tie_status(uuid)",
    "public.admin_actual_third_place_tie_revisions(uuid)",
    "public.admin_resolve_actual_third_place_tie(uuid,uuid[],text)",
    "public.admin_clear_actual_third_place_tie(uuid,text)",
    "public.process_due_entry_submissions(timestamp with time zone)",
    "public.get_entry_submission_status(uuid)",
    "public.get_leaderboard(uuid,integer,text)",
    "public.get_league_members(uuid,integer,text)",
    "public.search_league_transfer_candidates(uuid,text,integer)",
    "public.get_player_profile(uuid,uuid)",
    "public.get_h2h_rank_history(uuid,uuid)",
    "public.get_public_capacity()",
    "public.set_operating_limits(integer,integer)",
    "public.get_bonus_games(uuid)",
    "public.register_bonus_competition(uuid)",
    "public.withdraw_bonus_competition(uuid)",
    "public.save_knockout_prediction(uuid,smallint,smallint,uuid,integer)",
    "public.delete_knockout_prediction(uuid,integer)",
    "public.get_my_knockout_predictions(uuid)",
    "public.get_ko_predictor_standings(uuid,integer,text)",
    "public.save_lms_selection(uuid,uuid,integer)",
    "public.get_my_lms(uuid)"
  ],
  "notes": "Production deployments must not proceed unless the verified hosted database contract exactly matches this version. Contract 48 adds the bounded H2H rank-history read and is the production-published milestone. Contracts 49-52 add the Bonus Games platform and KO Predictor (ADR-0010 B2-B5). Contract 53 adds Last Man Standing (B6) in the tournament format: window-deadline picks, team-once-per-competition, win-to-survive groups / advance-to-survive knockouts, wipeout-void, with survival fully re-derived inside the single advisory-locked result operation. Development promotion follows Database parity; production remains locked at contract 48."
}
