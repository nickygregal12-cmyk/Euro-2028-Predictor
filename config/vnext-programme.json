{
  "schemaVersion": 1,
  "programme": "vnext",
  "currentStage": "14",
  "status": "in_progress",
  "lastMergedStage": "13",
  "productionCutoverAuthorized": true,
  "completionPredicate": "stages_8_through_15_merged_both_products_live_and_final_programme_audit_green",
  "carriedDebt": [],
  "completionContract": "docs/product/vnext-final-programme-completion.md",
  "stages": [
    {
      "id": "8",
      "name": "Matches + Match Centre",
      "status": "merged",
      "pr": 855,
      "contract": "current-stage-8-authority"
    },
    {
      "id": "9",
      "name": "Leagues",
      "status": "merged",
      "pr": 873,
      "contract": "docs/product/vnext-stage-contracts.md#stage-9--leagues"
    },
    {
      "id": "10",
      "name": "Player Profiles + H2H",
      "status": "merged",
      "pr": 891,
      "contract": "docs/product/vnext-stage-contracts.md#stage-10--player-profiles--h2h"
    },
    {
      "id": "11",
      "name": "Last Man Standing",
      "status": "merged",
      "pr": 903,
      "contract": "docs/product/vnext-stage-contracts.md#stage-11--last-man-standing"
    },
    {
      "id": "12",
      "name": "Predictor Championship",
      "status": "merged",
      "pr": 913,
      "contract": "docs/product/vnext-stage-contracts.md#stage-12--predictor-championship"
    },
    {
      "id": "13",
      "name": "Supporting Surfaces",
      "status": "merged",
      "pr": 923,
      "contract": "docs/product/vnext-stage-contracts.md#stage-13--supporting-surfaces"
    },
    {
      "id": "14",
      "name": "Football Hub Production Cutover",
      "status": "in_progress",
      "pr": null,
      "contract": "docs/product/vnext-stage-contracts.md#stage-14--football-hub-production-cutover"
    },
    {
      "id": "15",
      "name": "Euro 2028 vNext Adoption",
      "status": "not_started",
      "pr": null,
      "contract": "docs/product/vnext-stage-contracts.md#stage-15--euro-2028-vnext-adoption"
    }
  ],
  "resolvedDebt": [
    {
      "owedBy": "12",
      "kind": "backend_read_delta",
      "authority": "docs/product/vnext-championship.md#6-the-elimination-gap--closed-by-contract-208",
      "summary": "No season Championship read returned bonus_competition_entrants.outcome, so the surface could name a champion and never an eliminated player, and stated nothing rather than deriving elimination.",
      "resolvedBy": "supabase/migrations/20260819130000_cup_bracket_outcome_and_knockout_stage.sql",
      "resolvedEvidence": "get_season_cup_bracket returns your_outcome, the caller's own only; supabase/tests/254_cup_bracket_outcome_and_knockout_stage.sql; tests/database-parity/cupBracketOutcomeAndStageParity.test.ts; the mapper's three former fallbacks removed and the eliminatedInGroups world added.",
      "hostedRolloutRequired": true,
      "developmentVerifiedAt": "2026-08-19T23:32:57.276619Z",
      "hostedRolloutNote": "Development is verified at Contract 208 and Production is not. config/development-hosted-contract.json records the fast-lane run 32312618799 from main 0c48962, and an independent read-only Supabase query after it named 208 ledger rows with newest 20260819130000, the installed phase definition using the determinate split membership rule, and the bracket emitting the caller's own outcome. The flag stays TRUE because it covers every hosted environment and Production is still owed \u2014 that promotion is coordinated by its own ops change and is not this programme's to perform."
    },
    {
      "owedBy": "12",
      "kind": "backend_read_defect",
      "authority": "docs/product/vnext-championship.md#contract-167-is-the-initial-group-stage-and-that-is-a-scope-limit",
      "summary": "get_season_cup_phase (contract 120) selected the caller's membership with no phase_kind filter, no order by and no limit, so a split entrant holding two rows was silently answered about their PRE-SPLIT group. Called in production at src/services/supabase/seasonCup.ts.",
      "resolvedBy": "supabase/migrations/20260819120000_cup_phase_determinate_membership.sql",
      "resolvedEvidence": "the lookup names the caller's current phase, so the primary key identifies one row; supabase/tests/253_cup_phase_determinate_membership.sql proves a both-memberships entrant resolves to the split group under both a sequential and an index plan; tests/database-parity/cupPhaseDeterminateMembershipParity.test.ts.",
      "hostedRolloutRequired": true,
      "developmentVerifiedAt": "2026-08-19T23:32:57.276619Z",
      "hostedRolloutNote": "Development is verified at Contract 208 and Production is not. config/development-hosted-contract.json records the fast-lane run 32312618799 from main 0c48962, and an independent read-only Supabase query after it named 208 ledger rows with newest 20260819130000, the installed phase definition using the determinate split membership rule, and the bracket emitting the caller's own outcome. The flag stays TRUE because it covers every hosted environment and Production is still owed \u2014 that promotion is coordinated by its own ops change and is not this programme's to perform."
    }
  ],
  "productionCutoverAuthorizedAt": "2026-08-20T01:00:00.000Z",
  "productionCutoverNote": "Authorised by the repository owner on 20 August 2026, after the three preconditions this programme set were met rather than assumed. (1) EVERY HOSTED ENVIRONMENT IS LEVEL AT CONTRACT 208: Development verified 2026-08-19T23:32:57Z, Production verified 2026-08-20T00:45:00Z, both by an independent read-only query after a successful guarded rollout - so every read these surfaces make exists everywhere they run. (2) THE SWITCH IS PER DESTINATION AND PROVED IN BOTH POSITIONS: nine flags in src/app/routeFlags.ts, each selecting between a legacy route component that is still mounted and still passing its own tests and the vNext screen, so any one destination rolls back on its own with no data rollback. (3) THE BOUNDARY IS A DOOR RATHER THAN A HOLE: vNext reaches production only through src/app/vnext/, the public landing page's product preview and the routed /about, and tests/vnext/vnextProductionBoundary.test.ts fails on any other path. Turning a flag off is a build-time change and needs a redeploy; it needs no migration, no backfill and no data rollback."
}
