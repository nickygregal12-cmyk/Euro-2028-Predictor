# UI modernisation — execution authority

**Status:** active delivery sequence, adopted 5 August 2026; Domestic Frontend Alpha amendment accepted 7 August 2026.
**What it governs:** how the target design ([`hub-architecture-and-modernisation-plan.md`](hub-architecture-and-modernisation-plan.md), rev 1.5) and the approved tooling plan ([`../quality/open-source-improvements/`](../quality/open-source-improvements/README.md)) become production code.
**What it does not govern:** any scoring, lock, membership, settlement, progression or reveal rule. Like the design plan it sits under, this is presentation and delivery only, and it sits below the ADRs.

## Why this document exists

The design authority answered "what should the finished product look like" and the tooling plan answered "which quality systems are approved" — but neither was an owned item in the live work queue, and they disagreed about order:

- the design plan's §13.2 recommends foundations → public/auth → Hub shell → competition shell → Play inbox → Match Predictor;
- the roadmap's next executable sequence reads provider rehearsal → Match Predictor → LMS → Championship → Hub shell.

Left unreconciled, a future session could reasonably choose either route. This document records the deliberate reconciliation, so the two plans stop being passive documentation and become one delivery chain. The roadmap and [`../../MASTER-TODO.md`](../../MASTER-TODO.md) point here; if this document and the roadmap ever disagree again, fix the disagreement rather than picking silently.

## Domestic Frontend Alpha amendment — 7 August 2026

The owner has now named the next weekly-product milestone **Domestic Frontend Alpha**. This amendment is the current execution order for remaining frontend work and supersedes the 5 August top-level ordering in the next section wherever the two differ. The older section and progress table are retained because they explain what has already landed, why the existing tools/contracts were chosen and which implementation lessons must not be lost.

After currently active PRs settle, the delivery order is:

1. **Stabilise/apply the current Development contract batch** under the existing Development operating model. This is an operational prerequisite, not Production promotion.
2. **Canonical weekly route/navigation replacement** (`DFA-004`) — **partial; continue convergence**: converge on Hub → competition → game and one route-construction authority.
3. **Euro/tournament-route absence on the weekly site** (`EURO-001`–`EURO-004`): publication state/route guards first, then cards, discovery, landing content, metadata and other weekly surfaces.
4. **Deterministic parent/back navigation** (`DFA-005`) — **implemented**: every shipped deep route has an explicit logical parent and executable orphan-route coverage.
5. **First-sign-in onboarding and personalisation** (`DFA-001`, `DFA-002`): followed domestic competitions, optional favourite team, independent game choice, private-play choice, interrupted-resume and pending-invite recovery.
6. **Reusable shirt-style club identity** (`DFA-003`) — **implemented**: one canonical-team component, accessible label/fallback and bounded pattern vocabulary, adopted on the first high-value domestic football surfaces.
7. **Truthful Scottish Matchweek 1 settled Development state** (`DFA-007`) — **partial**: real football result truth is proven; lock-consistent Match Predictor points evidence remains to close.
8. **Scottish Last Man Standing begins at Matchweek 2** (`DFA-007`) — **rehearsal proven**: join, real clubs, select/change before lock and reload persistence; no invented Matchweek 1 LMS history.
9. **Scottish Predictor Championship begins at Matchweek 2** (`DFA-007`) — **next critical Alpha proof**: the private read contract is merged to the repository; promote it through the guarded Development lane, then verify the signed-in #593 opponent/fixtures/phase/table journey and Match Predictor points feed.
10. **All three games usable for both domestic competitions**: joined first, available second; every game card has honest state, direct action, deadline/round where relevant, route and rules/help.
11. **Private create/invite/join for all three game types** (`DFA-008`): Match Predictor league, LMS competition and Predictor Championship, preserving server-owned limits/rules.
12. **Competition Play weekly-action aggregation** (`DFA-006`): one answer to “What do I need to do this week?” derived from each game's own authoritative state.
13. **Development competition administration** (`DFA-009`): visible readiness and guarded callers for setup actions the protected server authorities already permit; never a second rules engine.
14. **Match Centre engagement**: connect football state to the player's prediction and appropriate points/trends/league/LMS/Championship consequence while preserving provisional-vs-official truth.
15. **Final personalised Hub Home** (`DFA-010`): one primary urgent/next action, at most two compact secondary actions, then live football and relevant favourite/followed/rank/recap/private context.
16. **Lock the final signed-in visual language and representative product states** across phone/desktop, light/dark and reduced-motion states.
17. **Build the final public landing visual and scripted non-interactive phone preview** (`DFA-011`) from those settled signed-in states.
18. **Reminders and player history** (`DFA-012`): incomplete Match Predictor / missing LMS pick near lock first, then useful season history for all three games.
19. **Full phone-first Development acceptance**: prove first sign-in through repeat use as one coherent product journey.

Provider ingestion, CI, security and operations may continue in parallel where they do not silently reorder this milestone. Backend availability is not a substitute for a usable surface. Production promotion remains a separate controlled programme.

### Landing-page timing amendment

The public acquisition surface already implemented behind `VITE_UI_PUBLIC_LANDING` is retained as an **intermediate acquisition shell**. Its existence does not satisfy `DFA-011`.

The landing architecture/content hierarchy may be settled early, but the **final visual landing page**, especially the phone-framed product walkthrough, comes after the signed-in Alpha journeys and visual language settle. The final preview reuses the real product's shirt-style club identity, states and interaction patterns rather than becoming a mock design the application later has to copy.

The simulated phone is presentation-only: fixed/local demonstration data; no account/session dependency; no live competitive API calls; no prediction submission; no clickable links/forms or pointer/touch activation inside the simulated UI; no keyboard focus inside it; real Create account / Sign in CTAs outside it; subtle `Demo` / `Product preview` labelling; `prefers-reduced-motion` static state; pause while page visibility is lost; no rapid flashing or distracting perpetual motion. Prefer one coherent walkthrough that rests on a strong Hub state; a continuous loop has an external pause/play control. On phone, conversion order is headline → proposition → CTAs → product preview.

### Shirt-style identity target contract

The reusable shirt-style club identity is **implemented** under `DFA-003`; `docs/design-system.md` and the executable visual/accessibility contracts are implementation evidence. Remaining work is adoption breadth on later surfaces, not inventing a second identity component. The component keys from canonical club identity, supports a bounded solid/hoops/stripes/halves-or-panels vocabulary and primary/secondary colour combinations, always accompanies an accessible team name/label, has a neutral fallback, works in both themes, never becomes the sole identifier and remains row-height/layout stable. Initial adoption order is favourite-team onboarding → Match Predictor rows → Matches/results → Match Centre → LMS selection → relevant Championship contexts → other team selectors.

## The 5 August reconciled order — retained implementation history

The design plan's §13.2 order was adopted for *foundations*, and the roadmap's product priority was adopted for the *first functional journey*. The sequence below explains the work already carried out under that decision. For remaining work, the 7 August Domestic Frontend Alpha amendment above is authoritative.

1. Visual foundations and component gallery — tokens, primitives and states in the production design system, proven in `src/dev/ComponentsPreview.tsx` before any product risk.
2. Thin global and competition shell foundations — enough navigation and context to host one new journey, not the full Hub experience.
3. Phone-first season Match Predictor — the first reversible product slice, behind a route-level feature flag with immediate rollback (§13.3).
4. Matchweek, monthly and form standings.
5. LMS weekly selection.
6. Predictor Championship surfaces.
7. Full Hub action and social experience.
8. Public acquisition (Appendix E) and remaining journey migration.
9. Legacy route retirement, by journey and only after the rollback window closes (§13.5).

The §13.2 phases the reconciliation *moved later* were the public/auth/onboarding shell and the full Hub Home: they built acquisition and hierarchy before any player could do anything new, whereas the season Match Predictor was where the backend was already ahead of the surface and where recurring save/lock behaviour — the highest-risk interaction pattern — got proven earliest. The 7 August amendment intentionally moves first-use onboarding forward again, now that enough real domestic product/state exists for onboarding to lead into a coherent Hub rather than an empty shell.

**Provider rehearsal runs in parallel, not in front.** The roadmap's provider rehearsal blocks provider-fed production behaviour; it does not block tokens, components, fixture-backed UI states or shell work, and the design plan expressly allows frontend design work to overlap backend stabilisation. Every journey built before rehearsal completes must render its fixture-backed and unavailable states honestly rather than assuming a feed.

## The premium prototype is a reference, not a starting point

`src/premium/` (`PremiumApp.tsx`, `premium.css`, `store.tsx`, `data.ts`, `types.ts`) is a parallel mock application: its own router, mock store, mock data, modal system and Lenis smooth-scroll loop. Nothing outside `src/premium/` imports it — `src/main.tsx` mounts the production `App`, so the prototype is unreachable from the real application. `tests/design/premiumPrototypeBoundary.test.ts` pins that boundary.

It must not be wired into production. Doing so would introduce a second router, a second component language and — because it uses the provisional Touchline brand while brand selection remains deferred under ADR 0019 — an unauthorised brand decision. Its value is as a visual reference from which selected patterns are extracted into the production design system.

| Prototype pattern | Disposition |
| --- | --- |
| Neutral surface/border ramps, radii, type scale, spacing, tabular numerals | **Extract** — re-derive as production tokens in step 1; do not copy `premium.css` wholesale |
| Card, row, status, alert, modal and navigation compositions | **Extract** — rebuild as production primitives against the existing token system |
| Framer Motion sheet/state transitions | **Extract selectively** — shared motion tokens with reduced-motion behaviour; ordinary hover and colour changes stay CSS |
| Lucide icon usage | **Extract** — behind project icon wrappers, per the tooling plan |
| Touchline brand marks, names and copy | **Discard** — brand selection is deferred (ADR 0019) |
| Prototype router, mock store, mock data, modal manager | **Discard** — production journeys use the real application architecture, typed read models and commands |
| Lenis smooth scrolling | **Discard** — a whole-page scroll hijack fights the browser's own scrolling and the accessibility behaviour that depends on it. (It was also named a suspect in the performance finding below; measurement cleared it, and the disposition stands on its own merits.) No production surface may adopt it without route-level performance evidence |

`lenis` is currently a production dependency imported only by the unreachable prototype; the Knip baseline below is expected to surface it, and it must not ship in the production bundle.

## The component and state contract harness

`src/dev/ComponentsPreview.tsx` is the declared harness for component and state contracts. No second component workshop (Storybook or similar) is introduced. The first-gallery scope for step 1:

- app and competition navigation;
- action/deadline cards;
- prediction fixtures;
- standings rows;
- loading, empty, partial, locked, offline, unavailable, conflict and error states;
- hostile long names and three-digit values;
- phone, desktop, light, dark and reduced-motion modes.

The existing static prototype contract (`tests/design/landingPrototypeContract.test.ts`) protects the HTML landing prototype; the gallery work extends equivalent protection to the production React design system, which the prototype contract does not cover.

**Foundations landed.** The twelve-step neutral ramp, three surface levels, separate border ramp, six-step type scale with tracking, tabular-numeral token, motion contract, stacking order and the third radius are in `src/styles/tokens.css`, documented in [`../design-system.md`](../design-system.md) and rendered in the gallery. `tests/design-system/foundationTokens.test.ts` holds the properties that make them a system: every ramp step further from the page background than the last, every text step above AA on every surface, borders never drawn from a text or accent value, routine motion under 300ms, and one ordered stacking scale.

Two decisions worth carrying forward. First, **nothing in production consumed the new tokens at the time they landed** — they sat beside the palette in force so the target could be reviewed on screen before components adopted it, and adoption then proceeded one reviewable change at a time rather than as a single unreviewable re-skin. *(That sentence described 5 August and no longer describes today: UI-11 repointed the seven neutral tokens at the ramp, and UI-06 → UI-09 took the type scale through every stylesheet. The staging worked as intended and the note is kept because the reasoning still governs what is left.)* Second, the derivation is genuinely new rather than a rename of the current values, as §11.1's re-derivation rule requires; the live palette is untouched precisely so that its documented WCAG remediation is not disturbed while brand selection remains deferred under ADR 0019. The full re-hue, if the brand decision calls for one, replaces the ramp's values and inherits its structure.

## Approved systems and prohibited duplicates

Approved — already present, formalised by the tooling plan's source register:

- **Lucide** — the only generic icon family, behind project wrappers; bespoke Predictor marks stay.
- **`flag-icons`** — generated configured subset, corrected to 4:3, with fallback and coverage tests.
- **Framer Motion** — sheets, layout/state transitions and signature moments only.

Prohibited, deliberately, to prevent a second UI language: Heroicons, Bootstrap Icons, Ionicons, Hover.css, Animate.css, any second component workshop, any second motion system. The tooling plan evaluated these and did not approve them.

## Tool phasing

**Use now** (steps 1–2): Playwright visual contracts against stable `ComponentsPreview` anchors with fixed time and fixture data; Knip in report-only mode; Lighthouse CI baseline and ratchet.

**Use with the first functional journey** (step 3): MSW fixture scenarios for loading, offline, unauthorised, malformed and delayed requests; Better Specs conventions for new Vitest/Playwright tests (guidance, not a dependency); fast-check for scoring, ranking and state invariants the new application contracts touch.

**Add later, separately:** Stryker (narrow scheduled mutation checks on pure scoring/lock/ranking code); k6 (before closed-cohort exposure or recruitment increase); Renovate, Dependency Review, Scorecard and SBOM as repository-hygiene PRs that must not delay the first UI slice.

The tooling plan's [`05-implementation-sequence.md`](../quality/open-source-improvements/05-implementation-sequence.md) remains the per-PR detail; this section fixes *when* each phase runs relative to the UI sequence.

## Knip baseline before extraction

The report-only Knip baseline runs before significant pattern extraction, because it will expose the unreachable premium files, prototype-only dependencies such as `lenis`, unused exports, duplicate icon implementations, obsolete transition code and — just as important — the genuine entry points that must be protected. The setup PR deletes nothing. Its deliverable is a classified report: production entry point; development/test harness; reference prototype; historical evidence; confirmed dead code; requires investigation. Cleanup follows as separate, reviewable changes.

**Done.** [`../quality/knip-baseline.md`](../quality/knip-baseline.md) holds the classified findings; `knip.json` holds the entry points and `tests/scripts/knipConfiguration.test.ts` keeps them honest. It confirmed the prototype's unreachability independently, confirmed `lenis` as a production dependency shipping for nothing, and found one real defect en route — the accessibility suites were importing `axe-core` as an undeclared transitive dependency. Its most useful output for the work that follows is a caution: most "unused exports" are barrel re-exports or module-local constants carrying a needless `export`, not dead logic, so the raw report is not a deletion list.

## The performance finding that gated prototype adoption — measured, and it was not the code

The design-authority PR's deploy preview recorded a Lighthouse performance score of **20 — down 76 points from production — while accessibility remained 100.** This section originally listed the likely suspects: the full-page animation load, the Lenis smooth-scroll loop, the oversized prototype stylesheet, font loading, and how much Framer Motion code loads on the landing route. **Every one of them was wrong**, and the reason is worth keeping rather than quietly deleting.

Three measurements settle it:

| What was measured | Performance | Accessibility |
| --- | ---: | ---: |
| Deploy preview of the design-authority PR (documentation only, no runtime code) | 20 | 100 |
| Deploy preview of the Knip baseline PR (tooling and documentation only, no runtime code) | 21 | 100 |
| The same production build, served locally and audited directly | **86** | 100 |

Two consecutive pull requests that change **no runtime code whatsoever** score ~20, while the identical bundle scores 86 when audited outside the preview environment, with best practices and SEO at 100, total blocking time at 80ms and cumulative layout shift at zero. A runaway animation loop, a scroll hijack or a heavyweight motion bundle would show up as blocking time and layout instability; neither is present. The suspects also could not have been responsible in the first place, because none of them ship — the prototype is unreachable from the application and the Knip baseline confirmed its dependencies are unreferenced by the production graph.

So the deploy-preview score is a property of **where it is measured**, not of what was built. It is not a regression, it never gated anything real, and no prototype pattern is blocked by it.

What this changes for the Lighthouse CI work: audit **locally built, fixture-backed routes** rather than the deploy preview, or the tool measures preview infrastructure and reports it as product quality — the exact mistake this section made until it was measured. That is now how it is configured, and the recorded baseline is `/auth/login` 89, `/auth/signup` 94 and `/auth/reset` 95, with accessibility 100 on all three: [`../quality/lighthouse-baseline.md`](../quality/lighthouse-baseline.md).

The gate this section used to impose on prototype adoption is therefore **closed**. Nothing is blocked by it. What survives is the route budget work itself, where the two opportunities worth carrying forward are render-blocking resources and unused JavaScript, at roughly 450ms each.

Lighthouse budgets are route-specific, not one universal score. Performance starts advisory; broken routes, inaccessible names and severe structural/accessibility failures block immediately.

## Acceptance criteria and ownership

| Artefact | Acceptance | Owner of record |
| --- | --- | --- |
| Visual baselines | Playwright screenshots against stable `ComponentsPreview` anchors, fixed time and fixture data, covering light/dark, phone/desktop and reduced motion; diffs published on failure; baseline changes reviewed like code | The PR that changes the rendered surface |
| Lighthouse reports | Deterministic locally built fixture-backed routes; route-specific budgets recorded with the baseline; performance advisory until stable, severe functional/accessibility failures blocking from the start | The PR that adds or regresses a route |
| Generated flags | Flag subset generated from configured team/venue data, 4:3 corrected, unknown-code fallback rendered and coverage-tested; no silent missing flag | The PR that changes team/venue configuration |
| Knip report | Report-only; every finding classified into the six categories above; intentional ignores narrow and justified in place | The baseline PR, then any PR that adds an entry point |

Each first reversible product slice (step 3 onward) additionally carries the design plan's §13.4 release gate: typed page read model, explicit commands, no raw Supabase access from components, layout-shaped skeletons, optimistic save state, conflict and unknown-outcome recovery, authoritative lock explanation, MSW-driven failure scenarios, telemetry distinguishing old and new UI, and immediate rollback to the old route.

## Progress against this sequence

| Step | State |
| --- | --- |
| Activation: reconciled order, prototype classification, harness and approved systems | **Done** — this document, with `tests/design/premiumPrototypeBoundary.test.ts` |
| Knip report-only baseline with a classified report | **Done** — [`../quality/knip-baseline.md`](../quality/knip-baseline.md) |
| Visual foundations in the production design system, rendered in the gallery | **Done** — `src/styles/tokens.css`, `tests/design-system/foundationTokens.test.ts` |
| Performance evidence and the preview-score investigation | **Done** — [`../quality/lighthouse-baseline.md`](../quality/lighthouse-baseline.md); the gate is closed, see above |
| Playwright visual screenshot contracts | **Harness built; baselines outstanding by design** (UI-19). `playwright.visual.config.ts` and `e2e/visual-gallery.spec.ts` photograph thirteen curated gallery sections at two pinned widths in both themes, addressed by `data-section` anchors derived from section titles rather than by hashed CSS module class names. The gallery gained the pinned widths this needs: `?width=phone` and `?width=desktop` fix the panels at 390px and 1280px, because their normal `flex: 1 1 340px` photographs the window rather than the component. `.github/workflows/visual-contracts.yml` is dispatch-only and carries the bootstrap: one run with `update_baselines` renders the images **on the runner that will later compare them** and uploads them for review and commit; adding the `pull_request` trigger is the step after that. It is deliberately absent from `ci.yml` and the browser suite, since a screenshot suite with no baselines fails on contact. `tests/design-system/visualContractHarness.test.ts` holds the determinism decisions — pinned widths, disabled animations, `deviceScaleFactor: 1`, no retries, a tolerance tight enough to catch a structural change — so relaxing one is a visible edit |
| Adopt the foundations component by component | **Done for the ramp, the type scale and the stacking order; one transition and the DEV harnesses remain.** The type scale reached every stylesheet (UI-06 → UI-09), the seven neutral tokens were repointed at the target ramp (UI-11), and UI-15 took the stacking scale into the five places that are genuinely application layers, the last 21 off-scale font sizes in product surfaces, and the one micro transition. `tests/design-system/foundationAdoption.test.ts` is the control that was missing: it relates the tokens to their consumers, so an unadopted literal now fails rather than passing quietly, and each remaining exclusion is listed there with its reason — crest monograms, a movement triangle sized as an icon, two local sibling stacks, the DEV harness stylesheets and `ProgressBar`'s 300ms width transition |
| Close the gallery's missing states | **Done** (UI-16, UI-17). All six states the harness could not reach now have sections of their own — offline, unavailable and conflict, then refreshing, stale and the blocking error page. `tests/design-system/galleryStateCoverage.test.ts` relates the harness to the §9.1/§9.2 matrix, which is the control that was missing and the reason states could stay absent while seventy sections made the gallery look complete; it holds the matrix count, so a state added to the authority without a section fails. It earned itself immediately, catching a section titled "error, blocking" against a matrix key of "error blocking" |
| Thin competition shell and the phone-first season Match Predictor, behind a route-level flag | **Registered at `/competitions/:competitionSlug/:seasonSlug/main-predictor`, still behind `VITE_UI_SEASON_MATCH_PREDICTOR`, which is off everywhere.** The page, shell, state machine, save/conflict recovery, lock-explanation layer and telemetry category were production code already; what was missing was the two facts a route needs and no browser can obtain — which season two URL slugs mean, and which matchweek that season opens at. Contract 121's `get_season_play_context` answers both in one read, `SeasonMatchPredictorRoute` composes it with the card gateway, and the route table, Netlify status rule, route title, telemetry category and hub dashboard link all name it. The flag is not set in `netlify.toml`, deliberately: contract 121 is applied to no hosted environment, so a preview with the flag on would render a failed read rather than a card. **Flipping the flag therefore waits on the migration reaching development, not on the UI.** The axe matrix defers it with the two harness blockers named — the flag, and a seeded season entry for the harness user |
| Matchweek standings (step 4) and LMS weekly selection (step 5) | **Done** — `SeasonStandingsPage` (UI-10) over `get_season_leaderboard`, and `SeasonLmsPage` (UI-13) over contract 116. Like the Match Predictor above, both are production code reachable only from their DEV harnesses until the routes are registered. **Monthly and form standings were not part of this and could not be built at the time**: `get_season_leaderboard(uuid, integer, text)` takes a competition, a page size and a cursor, and has no period argument at all, so a monthly or last-five table had no read to render. That was a migration rather than a component, and contract 122 has since landed it — see the retention-views row below |
| LMS registration (step 5, second half) | **Done for joining; leaving is deliberately not offered.** `SeasonLmsPage` had been telling a non-entrant "Join Last Man Standing to make a pick" while offering no way to join — a refusal naming an action the interface did not provide. `SeasonLmsRegistration` closes it over RPCs that already existed and needed no migration: `register_bonus_competition` resolves a competition by id and delegates to `join_competition_game`, holding no tournament-only join, and the games hub read that supplies the registration instants was widened for seasons by contract 118. Three decisions carry: states resolve from stored instants against the **server's** instant rather than the browser clock, so a wrong device clock cannot offer a closed window; the competition is supplied by the caller and matched by **exact id**, never searched for by game key, because contract 107 creates a successor competition on a wipeout restart and "the season's LMS competition" is an instance-resolution question contracts 103/104 own server-side; and `lmsRegistrationRefusal` is a **separate classifier from `lmsRefusal`**, which reads the same `55000` as "you have been eliminated" — wrong for a join. `55000` covers five distinct join rules (finished, not opened, closed, disqualified, no-rejoin), so its copy claims none of them and the panel reloads, letting the stored instants explain what actually happened. **Withdrawal is not offered**, and that is a recorded boundary rather than an oversight: `leave_competition_game` refuses once a `bonus_score_events` row exists for the caller, and no browser read exposes that fact, so a Leave control would be one whose availability the surface cannot honestly predict. It needs a read before it needs a button |
| Predictor Championship surfaces (step 6) | **Phase and group table built; the rest of the game has no read to build on.** `SeasonCupPhasePage` renders contract 120's bounded phase read through `createSeasonCupRpcGateway`, at `/dev/season-cup` until the routes are registered. Three decisions are worth carrying: the split is stated as ADR 0014 defines it — the same competition continuing with a narrower field, points carried through, nobody eliminated — because a player watching their group halve will otherwise read it as an elimination; opponents appear as *an entrant at a rank*, since contract 120 deliberately joins no profile and a surface that worked around that would decide a disclosure question it has no authority over; and a load failure renders as a failure with a retry rather than an empty table, which is the empty-versus-failed line the repeated browser-read contracts exist to hold. The split scenario in the DEV harness is currently the only place that rendering can be reviewed at all, because the phase-transition driver that would populate a real split group is unbuilt. Entry, fixtures/opponents, tie settlement status, the Penalty Number journey, the knockout bracket and private management/public discovery all still wait on drivers and reads that do not exist |
| Public acquisition landing page (Appendix E) | **Done as an intermediate shell, behind `VITE_UI_PUBLIC_LANDING`; not final visual Alpha acceptance (`DFA-011`).** Brought forward from step 8 on owner direction: until this shipped, an anonymous visitor at `/` was redirected to `/auth/login`, so the first thing anyone saw of the product was a password field for an account they did not have. `src/features/landing/` renders E.3's eight surfaces in order, in the production design system rather than the prototype's deferred Touchline brand. Guards: `tests/features/landing/`, `tests/app/publicLandingRoute.test.tsx`, and `/` added to `e2e/axe-unauthenticated.spec.ts` — the only harness that can see the signed-out root |

| Competition dashboard entry (step 7, first slice) | **Done: the dashboard reads real membership and its entry control works.** Two defects sat on it after the Hub itself became real. It rendered the *static* catalogue's `joined` flag, so it could say "Joined" while the Hub — reading the same season from the database one tap away — said the opposite; and its Join/Leave button was rendered **enabled with no `onClick` at all**, so pressing it did nothing and said nothing, which is worse than a disabled control because a player cannot tell a broken button from a refused action. Neither needed a migration. `get_competition_games` has always returned each game's `id`, registration window, `completed_at`, `allow_rejoin` and fuller membership row, plus a top-level `server_now` — the decode layer was discarding all of it, and the discarded `id` is precisely what a join is addressed by. `decideGameMembership` now resolves join/rejoin/leave from those stored facts against the **server's** instant, and states a refusal as a sentence rather than rendering a control the server would reject: no dead buttons. What it cannot know it does not claim — `leave_competition_game` also refuses once a `bonus_score_events` row exists for the caller, which no browser read exposes, so leaving is offered on the facts available and its refusal reported honestly. `gameMembershipRefusal` keeps `55000` deliberately vague because *both* writes overload it — joining across five rules, leaving across three — and the reload after every attempt is what actually explains it. A failed read is shown as failed and claims membership neither way, because the catalogue's own constant would otherwise assert a state the server never confirmed |

| Production routes for the season game surfaces | **Done for standings, Last Man Standing and the Championship — they are pages now, not previews.** Each had been production code reachable only from a DEV harness, because nothing turned `/competitions/premier-league/2026-27` into the season and competition identifiers its gateway needs. `SeasonGameRoutes.tsx` resolves that from server data rather than from the slug: the catalogue maps the URL to the exact `tournaments.name` the C1 migrations created, and `fetchHubMembership` turns it into the season id and its games' ids. `competitions.slug` is not browser-readable, so deriving an identifier client-side would silently disagree with the server. The dashboard's game cards now open them, so "Build pending" is gone for both. **Main Predictor is deliberately still null**: its card opens at a matchweek, and resolving the next playable one is the play-context read that arrives with its own contract. **This also fixed a live HTTP 404.** All three route controls — SPA status, titles, axe — carried their own copy of a regex that only matched `<Route` and `path=` on the SAME line, so a Prettier-wrapped route was invisible to all three at once. `/competitions/:competitionSlug/:seasonSlug` was exactly that: React served it, no control required a Netlify 200 rewrite for it, and the catch-all answered **404 while the page rendered normally** — invisible to a visual check, visible to crawlers, monitoring, link previews and SEO. `tests/app/routeDeclarations.ts` is now the single extractor, reading each declaration's opening tag so wrapping cannot hide one, and `routeDeclarations.test.ts` pins the wrapped case that caused it |

| Championship entry, the shell on every season page, and the dashboard's promise removed | **Done.** Three things that each made a real page read as an unfinished one. **(1)** `SeasonCupPhasePage` told a non-entrant "You are not entered" and offered no way in — the identical dead end Last Man Standing had. Entry is `join_competition_game` for *every* game key, so rather than a second registration surface the existing model takes a `RegistrationCopy` and both games share one path; a test pins that the two differ in wording and in nothing else, because a per-game copy of the logic would be a second chance to disagree about one rule. **(2)** The three season routes now render inside `SeasonCompetitionShell`, as the Match Predictor route already did — competition identity should not appear and vanish between one game and the next. Standings claims `play` rather than `games`: §7.4 puts standings inside the *game* shell as one of the Main Predictor's own sections and the competition sub-nav has no entry for it, so of the five §7.3 sections it belongs with the game it describes. The shell is **not** rendered before the season resolves, because a masthead reading "Competition" asserts something untrue while the read is in flight. **(3)** The competition dashboard's intro paragraph — *"This dashboard **will** prioritise incomplete predictions, the next lock, live results, current ranks…"* — was a roadmap rendered as body text, telling a player nothing about their own competition. It is gone, along with its now-dead stylesheet rule; what belongs there is next action and next lock, and those arrive with the reads that can answer them rather than as a promise |

| Play, and two surfaces that stopped withholding what they already knew | **Done.** **Play** is §7.3's remaining reachable section — "actions for all games joined in this competition" — and it is what makes the sub-navigation worth having rather than one link beside four disabled words. It deliberately computes no deadline, rank or next action: each game's own read already answers that, and gathering four of them here would put a fifth copy of "what is due" in the browser beside the four that know. What it adds is the short list — of everything the competition offers, these are yours. A joined game whose surface does not exist yet is **listed without a link**, because hiding it would misreport membership and linking it would be a dead link; and the flag-gated Match Predictor destination is supplied by the caller rather than assumed by the model, so the flag stays the one place that decision is made. **Two surfaces were already loading facts they threw away.** The Last Man Standing round read has always carried `locksAt`, so a player could see a round was open without being told how long for — it now says when picks lock, and drops the line rather than guessing when the server sends no instant. The standings read has always carried the caller's own row, so a player arriving at a long leaderboard had to find themselves in it to learn their position — it now states it, in words, spelling a shared rank as "joint 2" rather than printing "=2", and says nothing at all for a player holding no settled matchweek. `formatInstant` is now one shared helper rather than a copy per surface |

| ADR 0012's retention views, and §7.3's last buildable section | **Done.** **Monthly and rolling form** are the two tables ADR 0012 names beside the cumulative one, and they had been exported, tested and called by nothing since contract 94; contract 122 gave them a browser read and this renders it. Three decisions carry. They are rendered **beneath** the season table and always carry the sentence saying so, because ADR 0012 is explicit that the cumulative total is the only ranking that decides a season and a monthly winner given equal visual weight is a rival claim to it. Switching period is a **server load, not a browser filter**: the two are different answers over the same settled scores, and deriving one from the other would put the retention rules in the client. And a failed monthly read renders as a **failure**, never as "no months yet" — the read raises deliberately when a settled matchweek has no play window, precisely so an operator learns the calendar was never derived, and an empty state would discard that. Opponents are unnamed for contract 120's reason: the read joins no profile, so a row is "You" or "Entrant" and the caller is found by their own entry id. `formatMonth` reads `YYYY-MM` as numbers rather than parsing it as an instant — contract 122 already resolved the month in the competition's own timezone, and `new Date('2027-01')` would shift it back across the boundary in any negative-offset locale, undoing the one decision the contract made. **Leagues** is §7.3's fifth section and the last one with a season read to build on: `create_game_league`, `get_my_game_leagues` and `join_league` are all keyed by `game_competition_id`, granted, and had never had a caller. A private league belongs to a *game*, not to a competition — ADR 0011 keeps each game's standings its own and `leagues.game_competition_id` says the same in storage — so the surface names the game its leagues rank rather than heading them "Leagues" and implying a competition-wide table. **No league opens into a standings table, and the page says why rather than leaving it to be found.** `get_league_members` ranks by `predictor_internal.standing_metrics`, which reads `score_events`, `matches` and `match_predictions`; a season's points live in `season_matchweek_scores` and reach none of them, so that read would return **every member of a season league on zero points with no error** — a plausible table that is entirely wrong, and the same shape as the defect contracts 86, 98, 116, 118 and 120 each fixed. It needs a migration; the surface renders what the game-scoped reads genuinely answer, and states the gap |

| Matches, and the last dead label in the competition sub-navigation | **Done, and it is the first surface in the product worth opening before joining anything.** §7.3's Matches section — "fixtures/results/table" — needed no new read: `get_season_matchweek_card` has always returned a matchweek's fixtures with their kickoffs, status and results, and `predictor_internal.season_card_context` **tolerates a caller with no entry** by design, returning `card_status: 'no_submission'` rather than refusing. Nothing had ever called it that way. The only caller wrapped it in lock policy, Joker allowance and the per-fixture version map — the *prediction* concerns — behind a flag, so a season's fixtures were readable the whole time and no surface asked for them. Verified against hosted development before the surface was built: as an ordinary account holding no season entry, matchweek 2 of the Scottish Premiership returns Dundee v Aberdeen, Kilmarnock v Celtic and Rangers v Hibernian with real kickoffs. **A thin decoder rather than the existing gateway**, because a browse surface that kept the version map would be one render away from sending a stale version on somebody else's behalf; it also drops `prediction` outright, since a fixture list carrying predictions would be a second entry surface with none of the lock rules that govern the first. **Days and times resolve in the competition's own timezone**, not the viewer's and not UTC — a Saturday 15:00 kickoff is Saturday to everyone who follows that league — while the 12-or-24-hour format stays the viewer's, because that is a reading preference rather than a fact about the fixture. The same split contract 122 made for the month a matchweek belongs to. **The clock is never an authority on whether a match finished**: a fixture is played because the server said `played`, never because its kickoff has passed, which would be wrong every time a match is delayed or abandoned. Every season fixture in development is `scheduled` — nothing can record a season result yet — so every score renders blank, and the surface **says so** rather than looking faulty. **The Games label stopped being a dead end.** Last Man Standing and the Championship both declared `section="games"`, which the shell rendered as an unavailable label: two pages highlighting a section the player could not reach. Overview and Games now share one destination, deliberately — §7.3 separates them, but the dashboard has no state to drive it yet, so the games catalogue *is* what Overview renders, and splitting them today would leave Overview empty and put the same list on two addresses. They separate when Overview has next action and next lock to show, which is a missing read rather than a missing page. `seasonDestinations.ts` now holds that map once, because when each route built it inline the one that was forgotten kept rendering a shipped section as unavailable |

| Making the season Match Predictor safe to switch on | **Done — two defects that each made the flag unsafe to flip, and neither was visible while the route rendered 404.** **(1) Its shell had no destinations at all.** `SeasonMatchPredictorRoute` rendered `SeasonCompetitionShell` with the prop omitted, so Overview, Play, Matches, Games and Leagues were five inert labels on the page a player spends most of their time on. This is precisely the drift `seasonDestinations.ts` was created to stop, and this was the route that had not adopted it — found by grep rather than by looking, which is the only reason it was found at all. **(2) Its masthead asserted things that were not true**, and then **(3) the page was a trap for anyone not entered.** The card supplied its own competition identity, so the loading state rendered "Loading competition" over the season label "Season" and the failed state rendered "Match Predictor" over "Season" — furniture claiming something untrue while the read was in flight and still untrue after it failed. Identity now comes from the route, which resolved the real competition before the page mounts. The trap is worse than it looks: `get_season_matchweek_card` answers a caller holding NO ENTRY with an ordinary card, because `card_status` is `no_submission` for both "entered, nothing saved" and "never joined" — so the card rendered, invited a full matchweek of predictions, and the first save was refused by `require_season_entry`. A registration panel now states membership before that happens, as the third caller of the generalised registration path rather than a fourth copy of it. **The registration gateway could not reuse the existing one**, and the reason is worth keeping: `seasonLmsRegistration.ts` reads `get_bonus_games`, whose payload is typed by `BonusGameKey` — `ko_predictor`, `last_man_standing`, `predictor_cup` — and since the fix that stopped one unmodelled game taking the whole hub read down, `bonusGamesModel` deliberately SKIPS a key it does not model. Asking the Games hub about `main_predictor` therefore returns nothing, and a panel built on it would have told an eligible player their game was not listed. `seasonGameRegistration.ts` reads `get_competition_games` instead, which does model it and whose decoder already carries the registration instants, `completed_at`, the caller's membership and the server's own clock. Extending `BonusGameKey` would have been the shorter fix and was not taken: `competitionModel.ts` records that the architecture document owns that union and the type is its transcription. Resolution is by game key and **refuses an ambiguous one rather than choosing** — contract 107 mints a fresh Last Man Standing successor on every wipeout restart, so more than one match raises instead of guessing at a dead predecessor |

| One stylesheet, because splitting it cost more than it saved | **Done — all CSS 44.5 KB to 29.8 KB gz, a third of the budget reclaimed, with no change to a single page.** The budget had 0.5 KB of headroom and the obvious suspect was duplication: 83 CSS modules, 172 rule bodies appearing more than once, 27.3 KB of redundant raw bytes — 12% of all module CSS. **That was the smaller half of the problem and measuring said so.** The 39 emitted chunks totalled 177.9 KB raw and 44.2 KB gzipped; the identical bytes concatenated into one file gzip to **29.2 KB**. Fifteen kilobytes was per-file compression loss rather than content. The mechanism is gzip's sliding dictionary — it must see repetition before it can encode it, so a small file never gets going. Chunks of 2 KB raw and over compressed at ratio 0.23; the eighteen chunks under 2 KB managed only 0.41, and `ConflictBanner.css` was the reductio at **86 bytes raw and 95 bytes gzipped**, larger compressed than not. `cssCodeSplit: false` was the whole fix. **What it costs is stated rather than glossed:** every visitor now downloads every route's styles, so first paint waits on ~29 KB instead of ~12. That trade is only defensible at this size — splitting CSS earns its keep above roughly 50 KB and the whole application is well under — and the note in `vite.config.ts` says what to do if that stops being true. Nothing about authoring changes; CSS Modules still scope per component. **The budget followed the measurement down**, 45 KB to 34: leaving it would have banked the saving as silent room and invited exactly the drift that caused the problem. Two things are recorded rather than fixed. All JS is now the tight one at 292.8 of 300 KB. And the rule duplication is still there — worth roughly 1.5 to 2 KB gz once deduplicated, and now much less urgent, though `.srOnly` in eight copies has no reason to ever vary |

| The global navigation stops leading into Euro 2028 | **Done.** The bottom bar was the one part of the interface that never got converted when this became a multi-competition platform. Its tab NAMES were already right — §7.2's rail contract is Hub, Predict, Leagues, Games and More — but Predict went to `/predict`, Matches to `/matches` and League to `/league`, and **all three are the Euro tournament's own journeys**: each reads `useTournamentData()`. A player who joined the Scottish Premiership tapped Predict in the bottom bar and landed in Euro 2028, while every season surface built this session lived under `/competitions/…` and could only be reached by going Home first and tapping through. Related to `EURO-001`, which the requirements register already records as accepted and **currently violated**. **A global tab cannot name one competition**, because a player may follow several and the platform stores no notion of a current one — §7.5 marks a favourite as optional and nothing persists it, so inventing "your competition" would be a preference this repository has never held. The three tabs now ask, over `/play`, `/fixtures` and `/leagues`, and asking is cheap. **One joined competition is still shown rather than redirected past**: a redirect saves a tap, hides which competition answered, and would make a screen the player had never seen appear the first time they joined a second one. Membership comes from the games hub read and never from `HUB_COMPETITIONS`, whose `joined` flags are presentation placeholders — a chooser built from the constant would confidently offer competitions the player never entered. **Nothing about Euro breaks.** Its routes keep their addresses and are reached from its competition dashboard, which is where ADR 0026 wants them anyway. Three browser specs navigated to the predictor through the bottom bar; two now go by address, because the predictor is what they test and the navigation is not, and `route-accessibility.spec.ts` — where the nav genuinely is under test — keeps its keyboard journey and simply lands somewhere else. Two route titles were renamed to keep every title distinct, following the convention already in that file: the competition-scoped play surface is "Competition play", and the Euro league hub is "Tournament leagues" |

Keep this table in step with [`../../MASTER-TODO.md`](../../MASTER-TODO.md), which holds the same items at task granularity. A sequence document that stops recording what has happened becomes a plan nobody believes.
