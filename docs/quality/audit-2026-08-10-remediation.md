# Audit remediation ledger — 10 August 2026 audit

> **What this file is.** The single source of truth for **remediation position** against the
> read-only audit dated 10 August 2026 (Rev 3). It records which audit items have been taken,
> what happened to each, and what the next session should pick up.
>
> **It decides nothing and holds no rule.** Every authority it names outranks it —
> [`AGENTS.md`](../../AGENTS.md), [`../../CLAUDE.md`](../../CLAUDE.md), the ADRs,
> [`current-status.md`](current-status.md), [`risk-register.md`](risk-register.md),
> [`../roadmap.md`](../roadmap.md), [`accepted-requirements.md`](accepted-requirements.md) and the
> machine contract records. Where this ledger and one of those disagree, **this ledger is the one
> that is wrong.** It is a work tracker, not a status document, and it must never be cited as
> evidence that something is implemented or hosted.
>
> The audit itself is an **external claim**, not an authority. Each item is verified against actual
> code at HEAD before anything is changed. `ALREADY DONE` and `REJECTED` are correct outcomes.

| Field | Value |
| --- | --- |
| Source | Read-only audit, 10 August 2026, Rev 3 — audited at `41ddb05b`, re-verified at `aa0c71f2` (the audit's stated commit, contract 148) |
| Ledger opened | 10 August 2026, at `main` = `2d8ffcb`, repository contract **151** |
| Working rule | One item per session, one pull request per item, verify before fix |
| Governs | Remediation position only |
| Does not govern | Implementation truth, hosted state, decisions, risk classification, execution order |

## How the register is annotated

The `ID`, `Item`, `Area`, `Who` and `Notes` columns are copied **verbatim** from the audit
remediation prompt pack. Where the pack contradicts a repository authority, the contradiction is
recorded in `Notes` as a trailing **`AUTHORITY CHECK`** clause, dated, with the authority that wins.
The audit's original wording is left standing in front of it and is not edited. **Nothing has been
silently reconciled**, and no item's substance has been changed on the strength of one of these
checks — a session taking the item still verifies it against code itself.

`Status` starts at `TODO` for every row except **AUD-06**, which the register itself instructs to
initialise `ALREADY DONE` and which is verified merged on `main`. That single deviation from
"initialise `TODO` for every row" is deliberate and flagged in Handover.

## Item register

| ID | Item | Area | Who | Notes | Status | Evidence |
| --- | --- | --- | :---: | --- | --- | --- |
| **AUD-01** | HIBP client-side leaked-password check (`pwnedPassword.ts`) in signup + reset forms. Fail open. | Security | Agent | Audit §4a. Pro toggle unavailable on current tier. **AUTHORITY CHECK (10 Aug 2026):** `risk-register.md` `AUTH-002` states its required closure as "enable leaked-password protection in both projects" — a hosted toggle. The audit says that toggle is Pro-plan-only and cannot be enabled on the current tier, so the register's stated closure is unachievable as written. The register wins on classification; this item does not close `AUTH-002` and must not be recorded as closing it. Whether an HIBP mitigation is accepted **in place of** the register's stated closure is an owner decision that has not been taken. | TODO | |
| **AUD-02** | Add `https://api.pwnedpasswords.com` to `connect-src` in `netlify.toml`, plus a Playwright assertion that the range request actually resolves. | Security | Agent | Must land with or before AUD-01 — the check fails silently without it. **AUTHORITY CHECK (10 Aug 2026):** widening `connect-src` to a third-party origin touches the CSP that `risk-register.md` `SEC-002` and the deploy header-parity smoke both govern. It is an outbound connection to an external service from the signup form; treat it as a change needing its own reasoning, not a config line. | TODO | |
| **AUD-03** | Raise minimum password length to 12; enable secure password change. | Security | **HUMAN** | Supabase dashboard, both projects. **AUTHORITY CHECK (10 Aug 2026):** consistent with `AUTH-002`'s required closure ("set a sensible minimum length"). `AUTH-002` additionally requires a deliberate policy decision for **existing** passwords, which this item does not cover and which changing the setting does not perform. | TODO | |
| **AUD-04** | Enable Dependabot alerts. | Security | **HUMAN** | Repository setting, not a file. **AUTHORITY CHECK (10 Aug 2026):** already tracked as the open remainder of `ACQ-R19` in `risk-register.md` (6 August record: "Dependabot *alerts* are a repository setting"). Not a new finding. | TODO | |
| **AUD-05** | Review `claude/backend-migrations-status-h300h1` — now **contract 149 feature work** (season league matchweek predictions, 252-line migration + 275-line pgTAP suite), not the docs branch it once was. | Feature | **HUMAN** | Owner review. Note its commit "Use a six-character invite code in the contract 149 suite" couples it to AUD-11. **AUTHORITY CHECK (10 Aug 2026):** the description is out of date for the **second** time. Contract 149 is **merged on `main`** — `supabase/migrations/20260810150000_season_league_matchweek_predictions.sql` and `supabase/tests/198_season_league_matchweek_predictions.sql` are both present at `5057512` (PR #661). The branch is now 3 ahead / 0 behind `main` carrying entirely different work: `adbd3e4` "Author the pinned Production 145 to 151 promotion pair" plus two corrections. That is **production rollout workflow** content, which hard rule 4 forbids an agent from touching. The AUD-11 invite-code coupling described here has already landed in the merged pgTAP suite. Owner review is still required — of different content than this row describes. | TODO | |
| **AUD-06** | ~~Rebase UI finalisation branch~~ — **CLOSED.** Merged as #659 on 10 Aug. | Hygiene | — | Initialise Status to `ALREADY DONE`. No work. **AUTHORITY CHECK (10 Aug 2026):** verified — `b89a4fb` "UI finalisation: responsive signed-in football platform (#659)" is on `main`, and `claude/predictor-ui-finalisation-mellse` no longer exists on the remote. | ALREADY DONE | `b89a4fb` on `main` (PR #659); branch absent from `git ls-remote --heads origin`, 10 Aug 2026 |
| **AUD-07** | Wire `npm run check:lighthouse` into CI against the deploy preview, with LCP / CLS / INP / TBT assertions. | Perf | Agent | Config and dependency already exist. **AUTHORITY CHECK (10 Aug 2026):** `docs/quality/lighthouse-baseline.md` exists in the repository and was not read this session — a session taking this item should read it first, and should check it against `ui-modernisation-execution.md`'s named performance-score investigation gate before choosing assertion thresholds. | TODO | |
| **AUD-08** | Wire `npm run check:dead-code` (knip) into CI, non-blocking first. | Testing | Agent | Flip to blocking in a later item once backlog is clear. **AUTHORITY CHECK (10 Aug 2026):** `docs/quality/knip-baseline.md` exists and was not read this session; the "current backlog" this row refers to is presumably recorded there. | TODO | |
| **AUD-09** | Add `SECURITY.md` with a disclosure route and response expectation. | Security | Agent | — **AUTHORITY CHECK (10 Aug 2026):** absence confirmed at `2d8ffcb`. Related open finding `REPO-001` (Low, "Licence/changelog policy absent", **Partial**) covers adjacent repository policy files; consider whether this item should update that row. | TODO | |
| **AUD-10** | Generate Supabase types; type the client as `createClient<Database>`; add a CI step that regenerates and diffs, failing if stale. | Backend | Agent | Closes `TYPE-001`. Highest value on the list. Expect a split. **AUTHORITY CHECK (10 Aug 2026):** the audit's "145 migrations" is stale — the repository is at **151**. More importantly, generation reads a **hosted** project, and `AGENTS.md` requires hosted inspection to default to read-only while no hosted claim may be made without fresh target-specific evidence. A generated file is only true of the environment it was generated against, and Development's applied contract is itself currently disputed (see Handover). | TODO | |
| **AUD-11** | `SEC-001` invite codes: CSPRNG, ≥10 chars, limiter covering previews and failed joins, minimal preview payload, rotation/revocation. | Security | Agent | Needs a migration → claim next contract. Hand over, do not apply. **AUTHORITY CHECK (10 Aug 2026):** the Prompt B instruction to treat this as `BLOCKED` until AUD-34 is **void** — the 149 collision is resolved (see AUD-34). `NOW.md` advertises **152** as the next free number; open pull requests must still be checked before claiming it. `risk-register.md` records `SEC-001` as reduced-not-closed by contract 145 and lists the same four closure requirements this row names. | TODO | |
| **AUD-12** | Pin all GitHub Actions to commit SHAs. | Security | Agent | Dependabot `github-actions` already configured to keep pins current. **AUTHORITY CHECK (10 Aug 2026):** tracked as the second open remainder of `ACQ-R19`, whose 6 August record states SHA pinning "could not be performed from the working environment, and inventing a hash would break CI rather than secure it". Any pin must be a real, resolved SHA. Production rollout workflows are covered by hard rule 4. | TODO | |
| **AUD-13** | Add `zizmor` and `actionlint` to CI. | Security | Agent | 21 workflows holding production credentials. **AUTHORITY CHECK (10 Aug 2026):** the audit's own §1 counts **22** workflows; this row says 21. Neither figure was re-counted this session. | TODO | |
| **AUD-14** | PWA via `vite-plugin-pwa`: manifest, service worker, offline shell, iOS meta. | UI/UX | Agent | Check CSP impact; `worker-src 'self'` already present. **AUTHORITY CHECK (10 Aug 2026):** `index.html` records PWA treatment as "parked in the roadmap". `docs/roadmap.md` is the only live execution sequence and its Domestic Frontend Alpha order (7 Aug amendment) does not list a PWA. Shipping it is a **reordering of the accepted sequence**, which is an owner decision, not an agent's. | TODO | |
| **AUD-15** | Add Stylelint with `stylelint-declaration-strict-value` rejecting non-token colours. | UI/UX | Agent | Enforces the rule `tokens.css` already states. | TODO | |
| **AUD-16** | Add CODEOWNERS. | Ops | Agent | Map paths to governing authority docs. **AUTHORITY CHECK (10 Aug 2026):** absence confirmed at `2d8ffcb` (no `CODEOWNERS` at root or in `.github/`). | TODO | |
| **AUD-17** | Second `npm audit` step at `--audit-level=moderate` including dev deps, non-blocking. | Testing | Agent | — **AUTHORITY CHECK (10 Aug 2026):** `ACQ-R19` ("incomplete dependency scanning") is the registered finding this reduces. | TODO | |
| **AUD-18** | Keyboard-only Playwright spec for the full prediction journey. | A11y | Agent | Tab order, Enter/Space, Escape from `ChoiceSheet`/`Modal`, focus return. **AUTHORITY CHECK (10 Aug 2026):** `A11Y-001` is **Partial** — "automated coverage exists, manual review remains". A keyboard Playwright spec is further automation; it does not satisfy the manual assistive-technology review that `A11Y-001` and `TEST-001` both still require. | TODO | |
| **AUD-19** | Add `web-vitals` reporting into Sentry; enable Sentry Replay at low sample. | Perf | Agent | Sentry already installed. **AUTHORITY CHECK (10 Aug 2026):** Session Replay records real user sessions. `PRIV-002` is open and Stage C2 is blocked by issue #272 pending independent data-protection review; enabling replay is a data-collection decision, not a configuration flag. `web-vitals` alone carries no such question and may be separable. | TODO | |
| **AUD-20** | Hosted assertion that `net` is never an exposed API schema, run against both environments. | Backend | Agent | Cheap half of `DB-004`. **AUTHORITY CHECK (10 Aug 2026):** matches `DB-004`'s stated closure exactly. Note the assertion must run read-only against hosted targets, and a passing run is evidence only for the environment and instant it ran against. | TODO | |
| **AUD-21** | `DB-003` FK indexes — **measure first**. Capture `EXPLAIN (ANALYZE, BUFFERS)` on settlement joins, then index only what the evidence names. | Backend | Agent | Do not index every advisory row. Evidence gate is the point. **AUTHORITY CHECK (10 Aug 2026):** the AUD-34 block is void; next free contract is **152**, subject to checking open pull requests. `DB-003` agrees the evidence gate is the point and adds that measurement should cover "joins, deletion checks and settlement jobs". Measurement belongs on disposable local Supabase or a read-only hosted session, never a hosted write. | TODO | |
| **AUD-22** | Make the hosted-contract follow-up workflow update the Netlify contract variable or fail with an actionable mismatch. | Ops | Agent | `OPS-011`. **AUTHORITY CHECK (10 Aug 2026):** `OPS-011` is resolved for the current value and open on recurrence, and its required closure is worded as this row is. Production's Netlify value must not be touched (hard rule 4, and `OPS-011` records production as deliberately untouched). | TODO | |
| **AUD-23** | Collapse the production rollout workflows into one parameterised reusable workflow taking `from_contract` / `to_contract`. | Ops | Agent | ~200 KB of near-duplicate YAML. Biggest maintenance win. Expect a split. **AUTHORITY CHECK (10 Aug 2026):** hard rule 4 forbids touching "any production rollout workflow", which is precisely what this item rewrites. The two instructions are in direct conflict and the conflict is **unresolved** — this item needs an explicit owner decision before a session takes it. It also overlaps `claude/backend-migrations-status-h300h1`, which currently carries an unmerged "pinned Production 145 to 151 promotion pair" (see AUD-05). | TODO | |
| **AUD-24** | Define and publish a supported production version. **Reduced 10 Aug** — the application was released at contract 145 (deploy `6a79b4d5`), so the acute lag is fixed. What remains is writing down a supported version rather than an indefinite freeze, and closing the release-smoke evidence gap the password perimeter caused. | Ops | **HUMAN** | `OPS-006`. Owner decision, not an agent task. **AUTHORITY CHECK (10 Aug 2026):** three-way disagreement, unreconciled. (1) The audit says the repository is at 148; it is at **151**. (2) `current-status.md` (status date 8 Aug) still states "the currently published production artifact is still the 30 July 2026 build from `8244b722`, a Contract-63-era application bundle" and "it did not promote the application" — which contradicts the audit and the merged release record `docs/ops/production-application-release-145.md` (`cf0520c`, PR #646). (3) `risk-register.md` `OPS-006` still reads "Keep production at 63 and paused", predating both. The live authority is stale on a fact its own repository records; correcting it is owner work and is **not** performed by this ledger. | TODO | |
| **AUD-25** | Realtime live match scores, with circuit breaker and polling fallback. | UI/UX | Agent | Single channel scoped to visible round, torn down on unmount. Recall the 2.6M-request incident. **AUTHORITY CHECK (10 Aug 2026):** `AGENTS.md` requires all browser Supabase access to go through `src/services/supabase/` and bounded RPC/read-model contracts, and holds that live/provider data is provisional with protected confirmation as the scoring gate. A realtime channel is a new direct-subscription surface and must not become a second source of match truth. Not present in the roadmap's Alpha sequence. | TODO | |
| **AUD-26** | Offline prediction queue: `navigator.onLine` banner, IndexedDB queue, flush on reconnect. | UI/UX | Agent | Depends on AUD-14. **AUTHORITY CHECK (10 Aug 2026):** the database is authoritative for locks and submission. A queued prediction flushed after a matchweek lock must be refused by the server, not accepted by the client — the queue cannot become a way to write past a lock. `UX-004`'s recovery path deliberately clears only Supabase auth keys because they are "the sole browser-persisted state this application has, verified rather than assumed"; an IndexedDB queue makes that statement false and that finding's evidence must be revisited with it. | TODO | |
| **AUD-27** | Make `recompute_tournament_scores()` incremental; keep full recompute as an admin repair tool. | Backend | Agent | `PERF-002`. pgTAP proving incremental == full. Migration → hand over. **AUTHORITY CHECK (10 Aug 2026):** the AUD-34 block is void; next free contract is **152**. `PERF-002` is "Open pending complete-volume measurement" — the register gates this on measurement first, which this row does not mention. This is a **scoring authority** change: `AGENTS.md` requires TypeScript, PostgreSQL, pgTAP and parity to stay aligned, and no scoring change without its authority and executable evidence. | TODO | |
| **AUD-28** | Migrate `PredictionsProvider` (918 lines) to TanStack Query. | Frontend | Agent | Large. Expect several sub-items. **AUTHORITY CHECK (10 Aug 2026):** adds a runtime dependency against a CI-enforced compressed bundle budget. `AGENTS.md` and the audit both forbid raising a budget to make a change pass. | TODO | |
| **AUD-29** | Replace `ComponentsPreview.tsx` (2,515 lines) with Storybook 9. | Frontend | Agent | Consider whether it subsumes `visual-contracts.yml` — that is a decision to record, not assume. **AUTHORITY CHECK (10 Aug 2026):** `DOC-003` records the gallery as "Open; development-only". The accessibility work recorded in `docs/roadmap.md` (PRs #319–#340) scans "every declared route plus the component gallery, component states no route renders" — so the gallery is a **load-bearing accessibility fixture**, not only a gallery. Removing it moves that coverage. | TODO | |
| **AUD-30** | Add `lefthook` pre-commit: `oxlint` + `tsc --noEmit`. | Testing | Agent | — | TODO | |
| **AUD-31** | Stryker mutation testing scoped to `src/domain/**`, weekly schedule only. | Testing | Agent | Validates the 94.7% domain coverage figure. | TODO | |
| **AUD-32** | Split `LandingPage.module.css` (1,174 lines); promote reusable patterns into the design system. | UI/UX | Agent | — **AUTHORITY CHECK (10 Aug 2026):** the roadmap's Alpha sequence item 17 holds the final public landing visual until the signed-in states are settled, and `ui-modernisation-execution.md` classifies the current landing as "an intermediate acquisition shell, not final visual acceptance". Restructuring it now may be work done twice. | TODO | |
| **AUD-33** | Triage the six open Dependabot branches. Merge the five routine ones as one pass; handle `framer-motion` 13.0.0 **separately**. | Hygiene | Agent | Major animation bump vs 14 `prefers-reduced-motion` files and committed visual baselines. Visual contract workflow is the gate. All six are 3 behind `main`. **AUTHORITY CHECK (10 Aug 2026):** all six branches confirmed present on the remote. "3 behind `main`" is stale — `main` has advanced five further commits since the audit (`aa0c71f2` → `2d8ffcb`), so each is further behind than stated. Merging is a `main`-write; hard rule 2 forbids an agent merging its own pull request, and these are Dependabot's. An agent can triage, rebase and report; the merges are the owner's. | TODO | |
| **AUD-34** | Resolve the contract-149 collision: either land AUD-05's branch or move the advertised next-free number so two branches never hold 149. | Ops | **HUMAN** | Blocks every migration item — AUD-11, AUD-21, AUD-27. Do this before any of them. **AUTHORITY CHECK (10 Aug 2026):** **resolved by events, not by this remediation.** Contract 149 landed on `main` as PR #661 (`5057512`), followed by 150 (#662) and 151 (#663). `supabase/migrations/` holds 151 files; `NOW.md` advertises **152** as the next free number. No branch now holds a contended number. The blocks this row places on AUD-11, AUD-21 and AUD-27 are void. A session must still check open pull requests before claiming 152 — the rule stands even though this instance of it has closed. | TODO | |
| **AUD-35** | Reconcile the two ops documents on the site perimeter: `production-application-release-145.md` is titled "behind Team SSO" while `netlify-deploy-access.md` records password protection with SSO off. | Ops | Agent | Documents are authorities here; a stale title reads as current. **AUTHORITY CHECK (10 Aug 2026):** there is a **third** document in the disagreement, and it is the live authority. `current-status.md` (status date 8 Aug) states "Team SSO is required for **all** deploy contexts" and `EURO28_DEPLOYED_DB_CONTRACT=132` in all four Netlify contexts. Reconciling only the two ops documents the audit names would leave the live status authority stating the superseded perimeter. `AGENTS.md` forbids rewriting dated evidence to look current: the release record is dated evidence and its history is preserved; the **live** authority is the one that gets corrected. | TODO | |

## Handover

Last session: 10 August 2026 — ledger bootstrap (no item taken) — ledger created; register transcribed with 15 authority contradictions recorded; nothing else changed.
Next item: **AUD-01**

Anything the next session must know:

1. **Take AUD-01 first, not AUD-05.** AUD-05 and AUD-04 and AUD-03 are `HUMAN`. Per Prompt B, skip them and say in the report that they are waiting on the owner. AUD-06 is `ALREADY DONE` and needs nothing.

2. **The AUD-34 block is void — migration items are not blocked.** Contract 149 is merged on `main` (PR #661), 150 and 151 followed, and `NOW.md` advertises **152** as the next free number. AUD-11, AUD-21 and AUD-27 are ordinary `TODO` items. **Still check open pull requests before claiming 152** — that rule is repository policy, not a consequence of the 149 collision.

3. **`AUD-06` was initialised `ALREADY DONE`, not `TODO`.** Prompt A says initialise every row `TODO`; the register's own AUD-06 row says initialise it `ALREADY DONE`, and `main` confirms the merge at `b89a4fb` (#659). The specific instruction and the verified fact agreed, so they won. This is the only row that deviates.

4. **There is a stranded hosted record on `origin`, and it is live `OPS-010`.** Branch `automation/development-hosted-contract-31417611501` is 1 commit ahead of `main` and changes only `config/development-hosted-contract.json`, raising Development from **145 to 151** (run `31417611501`, repository head `2d8ffcbbb90bc9a7562433b1fe583ab82d93996a` — current `main`). Nothing on `main` reflects it, so `NOW.md` and `current-status.md` both report Development at 145 while a verified record says 151. This is exactly the failure `OPS-010` describes: correct evidence that cannot reach `main`. **It is not a register item and must not be silently landed by a remediation session** — it is a hosted record, and `AGENTS.md` reserves those. Report it to the owner; it also means any item reasoning from Development's applied contract (notably **AUD-10**) is reasoning from a number under dispute.

5. **`claude/backend-migrations-status-h300h1` has been repurposed a second time.** It is 3 ahead / 0 behind `main` and no longer holds contract 149 work — its commits are `adbd3e4` "Author the pinned Production 145 to 151 promotion pair" plus two corrections. That is production rollout workflow content, which hard rule 4 puts out of an agent's reach, and it overlaps **AUD-23**. AUD-05's owner review is still needed, of different content than the register describes.

6. **AUD-23 has an unresolved conflict with its own hard rule.** Hard rule 4 forbids touching any production rollout workflow; AUD-23 is a rewrite of exactly those. A session reaching it should stop and ask rather than choose an interpretation.

7. **Several items are outside the accepted execution sequence.** AUD-14 (PWA), AUD-25 (realtime), AUD-26 (offline queue) and AUD-32 (landing CSS) are not in `docs/roadmap.md`'s Domestic Frontend Alpha order, which is the only live execution sequence. Taking them reorders an accepted sequence — an owner decision. The `AUTHORITY CHECK` clause on each says so.

8. **Nothing in this ledger has been verified against code beyond the contradictions recorded.** The presence checks performed this session were narrow and are named in the Evidence column where they were made: migration count (151), contract 149's migration and pgTAP suite on `main`, the six Dependabot branches, and the absence of `SECURITY.md` and `CODEOWNERS`. Everything else in the register is the audit's unverified claim. Step 1 of the loop still applies to every item.

## Rejected findings

*Empty.* A finding is recorded here when a session proves it wrong at HEAD, with the file and line
that disproves it. Rejecting a finding with evidence is a correct outcome, not a failure. An item
whose finding was already fixed is **not** rejected — it is marked `ALREADY DONE` in the register
with the closing commit in Evidence.
