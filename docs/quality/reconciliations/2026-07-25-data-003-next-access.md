# DATA-003 next access requirement

A complete implementation requires either a normal repository checkout or a connector action that can enumerate the full tree. Resume issue #72 from the existing branch once that access is available; do not infer missing constraints from partial known paths.
