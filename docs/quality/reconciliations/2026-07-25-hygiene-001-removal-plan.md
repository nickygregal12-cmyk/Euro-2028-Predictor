# HYGIENE-001 removal plan

**Date:** 25 July 2026

The current risk register records that `src/assets/vite.svg` is present and has no import in `src/`, `index.html` or `public/`. The remaining action is to remove the unused scaffold asset and let the established repository checks prove there is no hidden build dependency.
