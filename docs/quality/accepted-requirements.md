# Accepted requirements not yet implemented

| Field | Value |
| --- | --- |
| Authority | Primary |
| Status | Active |
| Last verified | 2026-08-11 |
| Governs | The stable identifier, owning decision, dependency and acceptance evidence for every requirement that has been **accepted and is not implemented** |
| Does not govern | Current implementation or hosted state ([`current-status.md`](current-status.md) and the machine contract records); execution order ([`../roadmap.md`](../roadmap.md)); the detailed active/parked inventory ([`../../MASTER-TODO.md`](../../MASTER-TODO.md)); defects ([`risk-register.md`](risk-register.md)); deliberate postponements ([`deferred-decisions.md`](deferred-decisions.md)) |
| Supersedes | None |
| Superseded by | None |
| Related work | Issue [#272](https://github.com/nickygregal12-cmyk/Euro-2028-Predictor/issues/272) (`PRIV-003`–`PRIV-007`); Domestic Frontend Alpha requirements `DFA-001`–`DFA-012` |
| Implementation truth | Every Accepted row is unimplemented by definition. A row leaves the active gap only when merged code, a migration, an executable test or verified hosted evidence exists for it — and it leaves by being *marked implemented here*, not by being deleted |

## What this file is for

An accepted decision that nobody implements is indistinguishable, six months later, from a decision nobody made. The ADRs record *what was decided*; the roadmap records *what happens next*; neither is a durable list of the gap between them, and requirements have been lost in that gap before.

This register is that list, and nothing else. It is deliberately short on prose: each row names the identifier, the authority that accepted it, what it waits on, and what would prove it done.

**It is not a backlog, a status report or a plan.** It states no contract number, no hosted claim and no commit — those move, and this file must not.

## Rules

- **One row per accepted, unimplemented requirement**, with a stable identifier that never changes or is reused.
- **The owning authority is the ADR or governance record that accepted it.** This register never decides anything; if a row and its authority disagree, the authority wins and this row is wrong.
- **Acceptance evidence is what would let the row be marked implemented** — a named artefact class, not a promise. "A route guard test" is evidence; "done" is not.
- **A row is marked implemented, superseded or rejected in place.** Deleting a row destroys the only trace that the requirement existed, which is the failure this file prevents.
- **Identifiers are unique across the repository.** `PRIV-001` and `PRIV-002` are already allocated in [`risk-register.md`](risk-register.md); new privacy identifiers continue from `PRIV-003`. `FEAT-*`, `PLAN-*` and `SAFE-*` belong to [`feature-baseline.md`](feature-baseline.md) and are not used here.

## Domestic Frontend Alpha

Authority: [ADR 0023](../adr/0023-hub-information-architecture.md), Domestic Frontend Alpha reconciliation accepted 7 August 2026. Detailed page/route behaviour: [`../architecture/hub-information-architecture.md`](../architecture/hub-information-architecture.md).

These rows capture stable accepted gaps. They do not duplicate `EURO-001`–`EURO-004`; Euro absence retains those existing identifiers.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `DFA-001` | First sign-in is a one-time guided setup across followed competitions, optional favourite team, per-competition game selection and private-play choice; interrupted onboarding resumes and a pending invitation survives auth/onboarding | Required account/display-name setup; page reads/writes for preferences and memberships | Browser journey covering first sign-in, interrupted resume, returning sign-in and pending-invite recovery | Accepted — unimplemented as the complete flow |
| `DFA-002` | One optional favourite domestic team is a changeable profile preference only; it may personalise prominence but never affects game membership, predictions, scoring, rankings, permissions or urgent-action priority | A persisted preference keyed to canonical team identity | Migration/service contract plus settings/onboarding UI and tests proving no competitive rule consumes the preference | Accepted — unimplemented |
| `DFA-003` | Reusable shirt-style club identity uses canonical team identity, bounded patterns/colours, accessible label, neutral fallback, light/dark support and layout-stable repeated rendering; it is never the sole identifier | Canonical domestic team identity and target design-system adoption | Reusable component, gallery/visual/accessibility contracts and adoption on the first high-value football surfaces | **Implemented** — reusable identity and high-value domestic adoption are covered by executable visual/accessibility contracts |
| `DFA-004` | Weekly game navigation converges on canonical Match Predictor/LMS/Championship routes beneath `/competitions/:competition/:season/games/`, with one typed/generated route authority rather than scattered string literals | Existing route migration and `EURO-001`–`EURO-004` for hidden tournament absence | Route declaration/URL-construction tests and redirects proving no permanent parallel weekly route tree | Accepted — partially implemented, target not complete |
| `DFA-005` | Every shipped non-root weekly route has a deterministic logical parent independent of browser history, including game, private-container, Match Centre, player/H2H, failure and not-found routes | `DFA-004` route convergence | Executable route/navigation matrix proving every shipped route has its required parent/fallback and useful Match Centre return context | **Implemented** — the deterministic parent model and exhaustive shipped-route matrix are executable |
| `DFA-006` | Competition/game cards expose meaningful state and direct action; Competition Play answers “What do I need to do this week?” from game-owned truth; each game has the accepted compact secondary navigation | Bounded game/action reads | Component/browser tests for Match Predictor, LMS and Championship action states plus Play aggregation and secondary navigation | Accepted — partially implemented: compact game secondary navigation is delivered, and Overview now answers "what needs doing this week" for Match Predictor and Last Man Standing from each game's own read (`competitionWeekModel`). The Championship joined it once contract 133's player view supplied a fixture with a lock instant — reported, never marked outstanding, since it is won by the Match Predictor points the player is already being told to earn. Each game card now also carries that game's own outstanding action and its deadline, from the same computation, so a card and the panel cannot disagree. Overview additionally shows the COMPETITION's fixtures — a slice of contract 139's window, sharing the Matches section's model rather than a second one — because the weekly panel reports what each game asks of the player and said nothing at all to a player who has joined none. The Leave control also stopped guessing: contract 140's eligibility read means it is withdrawn with the rule named where the server would refuse, rather than rendered and rejected on press. Each card's PRIMARY control is now the action itself where the game is asking for one — "Pick your club" rather than "Open game" — and stays a destination where it is not, so a complete game is not dressed as a task. What remains of the row is the cross-competition view, current rank and recap |
| `DFA-007` | Scottish Premiership is the first truthful Development rehearsal: real Matchweek 1 results feed normal protected scoring from lock-consistent seeded test predictions; LMS and seeded Predictor Championship begin at Matchweek 2 with no invented Matchweek 1 history | Current Development contract batch applied; protected result/scoring authorities; season competition setup | Seed/rehearsal evidence showing real MW1 football, reload-persistent MW2 LMS pick, Championship opponent/fixtures/phase/table and normal Match Predictor points feed | Accepted — partially rehearsed: real MW1 result truth and reload-persistent MW2 LMS are proven; Contract 133 is now applied and persistently verifies the seeded private Championship discovery/opponent/fixtures/phase/table/privacy boundary. Lock-consistent MW1 points evidence, the later ordinary MW2 Match Predictor points feed, and the real Team-SSO-protected signed-in desktop/phone Championship journey remain incomplete |
| `DFA-008` | First domestic release provides create + invite + join journeys for private Match Predictor leagues, private LMS competitions and private Predictor Championships, preserving existing creator limits and game rules | Existing server-side container/join authorities plus any missing reads/commands | Phone/browser journeys creating, inviting and joining each of the three container types without client-side rule duplication | Accepted — incomplete |
| `DFA-009` | Development exposes a visible Competition Admin readiness/setup journey for fixture/provider state, current matchweek, Match Predictor availability, LMS setup/current round, Championship launch/field/phase, result-confirmation readiness and refusal/review conditions | Protected admin reads/commands; current Development contract batch applied | Authorised/unauthorised browser coverage plus guarded preview/execute evidence for server-supported setup actions | Accepted — partially implemented: `/admin/season` exposes matchweek readiness, the four granted season writes (open a competition, confirm/correct/clear a fixture result) and every server refusal verbatim, with authorised browser coverage in `weekly-admin-access.spec.ts`. Provider calendar approval and entrant disqualification are granted server-side and deliberately absent — no browser read exposes the staged proposals or a competition's entrants — and the page names both gaps. Unauthorised browser coverage of the new route remains |
| `DFA-010` | Final signed-in Hub Home is personalised and action-led: one primary urgent/next action, at most two compact secondary actions, then live football, relevant personalisation/rank/recap/private activity/discovery | `DFA-001`, `DFA-006` and supporting reads | Phone/desktop browser contract demonstrating priority ordering across normal, urgent and live states | Accepted — current competition chooser is only an intermediate shell |
| `DFA-011` | Public landing delivery is two-stage: structure/content hierarchy may be defined early, but the **final visual** page and non-interactive scripted phone preview are built from settled signed-in product states late in the Alpha | `DFA-003`, `DFA-006`, `DFA-010`; settled signed-in visual language | Landing contract proving real CTAs outside the simulated phone, fixed/local demo state, no interactive simulated controls/session/API writes, reduced-motion/static state, visibility pause and representative real product visuals | Accepted — existing landing implementation is an intermediate shell, not final visual acceptance |
| `DFA-012` | Post-core-Alpha retention work prioritises server-state deadline reminders for incomplete Match Predictor / missing LMS pick, then player/game history for Match Predictor, LMS and Championship | Core playable Alpha and server-owned lock/action state | Reminder eligibility tests plus user preference; history reads and browser journeys for all three games | **Partially implemented** — contract 163 supplies the provider-neutral reminder delivery contract the row's first half needs: an idempotent ledger keyed on contract 162's action, preference enforcement at scheduling so an opted-out player is never queued, deduplication, deadline-safe scheduling, a retry policy with a terminal state, stall reclamation and a dry-run default. **No provider is chosen** — `SITE-007` blocks that on the brand decision — so nothing sends. Contract 172 then supplied the runner half the row asks for: `pg_cron` drives the scheduler and the stall reclamation, and deliberately **not** the claim, because claiming is what a sender does. `process_reminder_schedule` is called with no arguments so its `dry_run` default applies, and a source assertion refuses any scheduled command that passes `false` or names `claim_due_reminders`. Contract 172 also adds `admin_reminder_delivery_health` so an operator can tell an empty ledger from a dead scheduler, which are the same number. Contract 161 supplies the history half's discovery read. **A real send remains blocked on `SITE-007`**, and the player/game history SURFACES and the reminder eligibility browser journeys remain unbuilt |

## Two-site architecture, shared accounts and Euro acquisition

Authority: [ADR 0026](../adr/0026-public-site-separation-shared-accounts-and-euro-2028-acquisition.md), accepted 6 August 2026.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `SITE-001` | One repository and one shared Supabase backend serve both frontend sites | — | Holds today; recorded so a second backend cannot be introduced without reversing an ADR | Accepted — currently true, unbuilt as a control |
| `SITE-002` | Two separate frontend deployments, each with its own domain and build | `SITE-003` for the weekly domain | A second deploy configuration producing a distinct site from one codebase | **Implemented in the repository; unimplemented as a deployment.** `VITE_SITE_VARIANT` selects the product at build time (`src/app/site/`), and one commit now produces two genuinely different builds: brand, navigation order and wording, which game is primary and which are Bonus Games, the landing page, the document head, the sitemap, `robots.txt`, and whether the Euro tournament's routes are served at all. `tests/app/siteBuildOutput.test.ts` applies the real build transform to the real `index.html` for both. What remains is an operations task: a second Netlify site bound to the Euro domain with its own environment variables. Creating it is outside a frontend session's authority |
| `SITE-003` | The weekly platform uses the eventual umbrella-brand domain | [ADR 0019](../adr/0019-brand-decision-deferred.md) brand trigger (Phase 0 discovery) | Chosen brand, registered domain, site bound to it | **Owner-decided 18 August 2026: Predictor Hub, operationally.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 25 supersedes § 1's deferral. The operational name may be used now across internal surfaces, documentation and deployment naming. **Domain and trademark clearance is an OPERATIONS gate, not an engineering one** — public launch, the public domain and neutral-branded transactional email remain blocked until it is done, operational adoption is not clearance, and no implementer may register a domain or assert a mark to close a technical dependency. |
| `SITE-004` | Euro 2028 uses the purchased tournament-specific domain | `SITE-002` | Euro site served from the retained tournament domain | Accepted — unimplemented as a deployment, and no longer blocked by the codebase. `VITE_PUBLIC_ORIGIN` carries the domain into the canonical URL, the Open Graph URLs, the generated sitemap and `robots.txt`; a build with no origin configured emits none of them rather than falling back to the other site's domain. The remaining step is binding a Netlify site to the domain with `VITE_SITE_VARIANT=euro` |
| `SITE-005` | No permanently diverging Euro source branch; a temporary release branch is permitted near launch | — | Branch policy honoured at the Euro launch; no long-lived Euro branch exists | Accepted — in force as a prohibition |
| `SITE-006` | Both production frontend domains present in the Supabase Auth redirect configuration | `SITE-002`, `SITE-003` | Allow-list containing both origins, verified by a real confirmation and recovery send per origin | Accepted — unimplemented |
| `SITE-007` | The transactional sender moves to the neutral umbrella brand | `SITE-003`; [`../auth-plan.md`](../auth-plan.md) § 5 checklist | New sender domain verified, SPF/DKIM/DMARC valid, signup/recovery/email-change re-tested | **Owner-decided 18 August 2026, with [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 25.** No longer blocked on the NAME — Predictor Hub is decided. Neutral-branded transactional email still waits on the domain and trademark clearance gate, which is an operations task rather than an owner decision. |
| `ACCOUNT-001` | One Supabase Auth account and profile work across both sites | `SITE-002` | Same account signs in on both origins against one `profiles` row | Accepted — unimplemented (single-site today) |
| `ACCOUNT-002` | The same credentials work on both sites | `ACCOUNT-001` | Browser proof on both origins | Accepted — unimplemented |
| `ACCOUNT-003` | Separate browser sessions acceptable initially; seamless handoff assessed later | — | Recorded position; revisited by a new decision, not by drift | Accepted — in force |
| `ACCOUNT-004` | Signing up joins no competition, game or private container | — | Test asserting a new account holds zero memberships on either origin | Accepted — law holds today, unproven per-origin |
| `ACCOUNT-005` | Acquisition source is analytics metadata only, never authorization data | Recording acquisition source at all | Test asserting no policy, grant or visibility check reads the field | Accepted — vacuous until the field exists |
| `EURO-001` | Euro 2028 is completely hidden from the weekly platform until an owner-approved publication state | `EURO-002` | Weekly platform surfaces contain no Euro reference while the state is hidden | **Implemented in the repository and Development** — static weekly/public Euro references have been removed, Euro-only player routes are isolated behind `TournamentJourney`, and the route boundary consumes the server-owned publication state before tournament providers mount. Development is hosted with the publication state still `hidden`. **Partly advanced 11 August 2026 by ADR 0026's deployment seam, and partly not.** What IS closed: the Hub build's document head, sitemap and `robots.txt` are generated per deployment and name no tournament and no tournament domain, which retired the exception `euroAbsentFromPublicSurfaces` used to make for `euro28predictor.com`. What is NOT: `TournamentJourney` gained a deployment gate that refuses the tournament's player routes, and **both deployments still set `servesEuroTournament` true**, so the weekly platform continues to serve them. That was tried and reverted the same day — the browser suite showed that withdrawing them deletes the `euro-2028-baseline` return evidence (the tournament profile, a tournament league's pagination, the head-to-head) rather than moving it, because there is no Euro-variant browser configuration for those specs to run under, and that withholding `/admin/results` removes the only Results Centre there is. **The remaining work is therefore an owner decision plus a browser-suite move, not a control to design:** the refusal is built and proven against an explicitly-configured non-tournament site. The two gates are independent: deployment answers "is this the tournament's site", publication answers "has its owner published it", and the publication gate is unchanged and still enforcing. Production application release remains separately gated and is not claimed here. **Half of that remaining work is now built, 11 August 2026 — the browser-suite move, and not the owner decision.** `playwright.euro.config.ts` collects exactly the fourteen parked specs against a `VITE_SITE_VARIANT=euro` build, so "there is no Euro-variant browser configuration for those specs to run under" has stopped being true and the specs have somewhere to move TO rather than be deleted by the flip. It is **dispatch-only** through `.github/workflows/euro-browser-journeys.yml` and **cannot pass yet**: `src/App.tsx` registers none of the tournament routes these journeys drive and `tests/app/parkedEuroBoundary.test.ts` asserts their absence, so a pull-request gate would attach fourteen permanently-red checks to a suite nobody could make green. **`servesEuroTournament` is unchanged** — both deployments still set it true, and the flip is still the owner decision this row already records. One detail is load-bearing rather than cosmetic: the config declares the WEEKLY project names, because eleven of the fourteen specs open with `test.skip(testInfo.project.name !== 'desktop-chromium', …)` and an `euro-`prefixed name would skip all fourteen and report green having run nothing. `e2eProjectGating` now derives that check from the specs' own gates rather than from a copy of the list |
| `EURO-002` | Server-owned publication states: hidden, prelaunch, registration-open, live, completed, archived | — | Persisted state with one authority, and a transition record | **Implemented** — Contract 143 is hosted in Development with the singleton defaulting to `hidden`, bounded `euro_publication_state()` read, owner-only adjacent transitions through `admin_transition_euro_publication_state` and append-only transition history. Production hosting remains separately gated |
| `EURO-003` | While hidden, Euro absent from landing content, Hub discovery, competition cards, navigation, metadata, sitemap, Open Graph and guessable public routes | `EURO-002`, `EURO-004` | Each surface checked, including a direct request to a guessable Euro route. **On the landing surface this was one atomic change across five files** — the fifth being `index.html`, which this row used to omit — Appendix E of [`../design/hub-architecture-and-modernisation-plan.md`](../design/hub-architecture-and-modernisation-plan.md), [`../design/hub-landing-prototype.html`](../design/hub-landing-prototype.html), `tests/design/landingPrototypeContract.test.ts` (which pinned the Euro band's presence and order, and now asserts its absence) and `src/features/landing/`. Changing fewer leaves the prototype contradicting its own authority | **Implemented in the repository** — landing/prototype content and `index.html` metadata are clear of Euro, Hub discovery/cards/navigation/sitemap were already clear, and guessable Euro-only player routes now fail closed against server publication state. Development remains `hidden`; Production release is not claimed  **Extended 11 August 2026:** the sitemap, `robots.txt` and the document head are no longer three static files naming one domain — they are generated per deployment from `SiteConfiguration`, and `tests/app/euroAbsentFromPublicSurfaces.test.ts` now checks what the HUB BUILD SHIPS rather than what a file in `public/` says. That is why the row's domain exception is gone: the Hub build carries no domain at all until one is configured for it. **Extended again 11 August 2026, at the route rather than on a control:** `/auth/signup` was directly reachable on the Euro build in every publication state and served a working account-creation form, so a bookmark, a shared link, a search result or a typed URL created a real account for a tournament nobody had published — a hidden "Create account" button is not a control. `EuroSignupGate` now refuses the route, reads the same `registrationOpen` table the button reads so the two cannot disagree, fails closed on an unreadable state, and is a no-op on the Hub build because one Supabase project serves both deployments and a project-level signup switch would close weekly signup to close this one. Log in is deliberately not gated. |
| `EURO-004` | Visibility enforced by the server-owned state plus route guards, not client filtering | `EURO-002` | Route guard refusing the route, proven by test rather than by absence from a catalogue constant | **Implemented in the repository** — `TournamentJourney` reads `euro_publication_state()` and refuses hidden or unreadable player routes before tournament data/prediction providers mount; executable tests prove hidden refusal, read-failure refusal, post-publication access and the separately authorised hidden admin preparation exception |
| `AGE-001` | The initial external cohort is restricted to users aged 18 or over | — | Server-side signup rule, eligibility wording and matching test fixtures | **Rejected — superseded by an owner decision of 11 August 2026.** This is a free football predictor and not a betting product, so the 18+ restriction, the age gate and the date-of-birth field it would have required are withdrawn. The row is marked rather than deleted, as this register requires: it records that the requirement existed, was accepted, and was reversed by the owner rather than dropped by an implementer. **Do not implement it, and do not treat a surviving reference to it elsewhere as an active migration requirement** |

## Auth screen resilience

Authority: [`../design-system.md`](../design-system.md) § 6 Auth screens, amended 22 July 2026 by the interface audit of that date. The requirement was accepted then and its only record since has been an unchecked box in [`../auth-plan.md`](../auth-plan.md) § 3 — which is precisely the gap this register exists to close, and is why it is given an identifier here on 11 August 2026 rather than left where a reader would have to notice a checkbox.

`AUTH-001` and `AUTH-002` are allocated in [`risk-register.md`](risk-register.md) and are not repeated here; this continues that sequence, as `PRIV-003` continues its own.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `AUTH-003` | A Turnstile load failure is a designed state, never a dead call to action: the rejection is surfaced with a visible fallback and a reload offer, submit buttons are never disabled-until-valid, validation speaks on submit with per-field errors, and no submit handler returns silently | — | Tests proving a blocked Turnstile script reaches a visible fallback rather than a permanently disabled submit, and that submitting an invalid form produces per-field errors rather than nothing | Accepted — **unimplemented, and measurably so**: `TurnstileWidget.tsx` catches the `loadScript()` rejection with `.catch(() => onTokenRef.current(null))`, folding a blocked script into an ordinary absent token, while `LoginForm.tsx` and `SignUpForm.tsx` disable submit on `turnstileEnabled && !captchaToken`. A visitor whose ad-blocker or corporate network blocks the script therefore sees a permanently dead button and no explanation |

## Account closure and formal erasure

Authority: [`../architecture/stage-c1-c2-governance.md`](../architecture/stage-c1-c2-governance.md) § Stage C2, amended 6 August 2026. Tracked by issue [#272](https://github.com/nickygregal12-cmyk/Euro-2028-Predictor/issues/272), which stays open.

`PRIV-001` and `PRIV-002` are allocated in [`risk-register.md`](risk-register.md) and are not repeated here.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `PRIV-003` | Ordinary Close Account: delete the Auth identity and direct identifiers, clear private preferences, transfer or archive owned containers, retain only justified minimum pseudonymised history | `PRIV-007` | Closure routine, retention schedule and deterministic tests | Accepted — **hosted implementation blocked** |
| `PRIV-004` | No permanent public cross-competition former-player identity | `PRIV-003` | Placeholder is generic or competition-specific; test asserting no stable cross-competition public identifier | Accepted — blocked |
| `PRIV-005` | Formal erasure is a separate rights-request workflow with individual assessment | `PRIV-007` | Rights-request process, granular deletion path and standings recomputation | Accepted — blocked |
| `PRIV-006` | Settled Cup and LMS outcomes preserved deterministically, without restoring eliminated players or changing settled winners | `PRIV-005` | Neutral settled-outcome placeholders plus recomputation and audit tests | Accepted — blocked |
| `PRIV-007` | Qualified independent UK data-protection review, and the resulting LIA, DPIA, retention, privacy and process requirements, complete before any hosted Stage C2 change | External review | Signed independent review and the completed assessments it requires | **Blocked, and the missing evidence is now named individually.** Audited 12 August 2026 against issue #272 and the whole repository. The issue is **open**, and its most recent owner comment (6 August 2026) states in terms that "the block above stands unchanged", that nothing in it "may be read as approval", and that an AI-assisted assessment "is not the review and cannot close this issue". **Absent from the repository, item by item:** a named reviewer with role or qualification; a review date and the law/guidance version considered; an approved lawful basis and continuing purpose; a field-by-field erasure/retention decision; a retention schedule; required privacy-notice and user-flow wording; a DPIA decision; an LIA; required technical and organisational safeguards; residual risks with owner acceptance; and the explicit **approved / approved-with-conditions / not-approved** conclusion. A search for any DPIA, LIA or retention-schedule document returns none. **So `PRIV-003` to `PRIV-006` cannot proceed**, and nothing in this session touched Stage C2. A documentation test already asserts that **no legal approval is claimed** by any repository document, and that remains true — this row is a record of absence, not a substitute for the review. **Removing that phrase is what `acceptedRequirements.test.ts` refused during this audit**, which is the control working: the register may describe the gap in any words it likes, and may not stop saying the one thing that matters. **Restated 18 August 2026, unchanged.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 27 confirms this review is genuinely external and cannot be self-approved: not by analysis produced inside this programme, not by an implementer's reading of the regulations, and not by an AI-generated opinion. `PRIV-003` to `PRIV-006` stay blocked and Close Account and erasure work stays out of scope. ADR 0028 § 21 is a product judgement about what a leaderboard implies and discharges no part of this review. |

## Provider change handling

Authority: [ADR 0023](../adr/0023-hub-information-architecture.md) § Development competition administration, clarified 6 August 2026; [ADR 0020](../adr/0020-football-prediction-hub-product-model.md) § Ingestion.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `INGEST-001` | An existing, correctly mapped fixture's kickoff may be revised automatically under the delivered safeguards | — | Delivered; the revision import and its rescheduled-fixture lock are merged with pgTAP coverage | **Implemented** — retained here as the boundary the rest are defined against |
| `INGEST-002` | A newly discovered fixture requires administrative approval | `INGEST-005` | Proposal queued rather than created; test proving no automatic fixture creation | **Implemented** — contract 174. Measured first, in contract 117's own committed text: an unheld fixture incremented `v_unmatched`, that counter went into a jsonb report nothing reads, and no administrator ever saw it. Detection now stages a `discovered` proposal and `223` asserts that **no fixture is created or changed** by the automatic path. Creation happens only through `admin_decide_provider_change_proposal`. It deliberately does **not** claim to tell a changed team or round apart from a new fixture — `season_fixtures` holds no provider fixture id, so that distinction is not derivable; the proposal carries the provider's id and a `possible_change_of_an_existing_fixture` warning where a prior revision recorded it |
| `INGEST-003` | Removal, cancellation, abandonment, material identity change or material round change requires administrative approval | `INGEST-005` | Each class routed to review; test per class | **Implemented for postponement, abandonment, cancellation and removal** — contract 174, each routed to its own proposal kind through contract 135's status vocabulary, with an unknown token failing closed. **Removal needs a caller-declared complete span**: "the provider stopped sending it" is evidence only if the payload was complete over some window, and a poll is not — so `p_covers_from`/`p_covers_to` are required and the automatic consumer passes neither. Identity and round change are **not separately classified**, for the reason in `INGEST-002`'s row, and that limit is stated rather than papered over |
| `INGEST-004` | Ambiguous provider data fails closed | — | Delivered for the mapping and import path: an unmapped identifier fails the whole payload | **Implemented** for import; unproven for the approval workflow |
| `INGEST-005` | Approval and rejection retain provider evidence, operator, decision and resulting calendar change | `INGEST-002`, `INGEST-003` | Append-only record carrying all four, with an administrator surface | **Implemented** — contract 174's `provider_calendar_change_proposals` carries the decoded proposal, what we held, named warnings and blockers, the deciding administrator, the reason and the resulting calendar mutation, append-only by trigger and undeletable. `admin_provider_change_proposals` is the bounded administrator read, building on contract 168's shape and returning no raw provider payload. A decision is one-way: repeating the same decision is answered rather than refused, and a different one is refused, because the first decision's calendar mutation has already happened. **No frontend consumer**, so `/admin/season` still does not show the queue |
| `INGEST-006` | Provider data never becomes official result truth automatically | — | Delivered for the tournament path; **superseded for a league season** | **Superseded in part, 9 August 2026, by owner decision** recorded as an amendment to [ADR 0020](../adr/0020-football-prediction-hub-product-model.md) and implemented at Contract 135. A measured provider final status now writes the official result of a **league-season** fixture with no human action, through the audited writer, with an administrator's correction ending provider authority over that fixture. It remains **in force unchanged for the tournament path**: Euro 2028 results are confirmable only by a signed-in administrator, and no provider function may name `public.matches` or `public.match_result_revisions`. Not deleted, because the requirement still governs half of what it was written for |

## Operating limits

Authority: [ADR 0023](../adr/0023-hub-information-architecture.md) § Private containers (per-owner limits, unchanged) and § Operating-limit classes (added 6 August 2026).

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `CAP-001` | The global public-user and league ceilings are an operational circuit breaker, distinct from product limits | — | Recorded classification; the limits themselves are unchanged | Accepted — classification recorded, values unchanged |
| `CAP-002` | ADR 0023's per-owner active-container and creation-rate limits stand unchanged | — | In force server-side today | **Implemented and in force** |
| `CAP-003` | A per-league membership limit for ordinary private leagues; 100 members is a recommendation, not an approved value | Owner approval | An approved figure, then a migration and pgTAP coverage | **Implemented 12 August 2026.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 3 approved **100 members** as the initial ordinary private-league limit, and it is enforced by a trigger on `public.league_members` that takes a per-league advisory lock before it counts — on the table rather than in `join_league`, because `create_league` writes the owner's membership row directly and a cap in one entry point is a cap the next writer silently does not have. **No organiser exemption**, the ADR having declined to grant one, and no reach into Last Man Standing or the Championship, whose fields are not in that table. The configured ceiling is `service_role`-settable only and cannot be set above the approved figure or below the largest league that already exists. **Applied to no hosted environment yet**, which is the rollout rather than the requirement. |
| `CAP-004` | Commercial entitlement limits stay separate from the operating circuit breaker | A commercial model existing | Separate tables and authority when built | Accepted — unimplemented |
| `CAP-005` | Custom SMTP is configured through the Euro 2028 Predictor domain, so email delivery alone no longer justifies the public-user cap | — | Delivered and live-verified; see [`../auth-plan.md`](../auth-plan.md) § 5 | **Implemented**; the cap's remaining justification is `CAP-001` |
| `CAP-006` | Raising the public-user cap to 250 is the next recommended controlled test stage | Owner approval | An approved, executed change with hosted evidence | **Owner-approved 12 August 2026; unimplemented, and it needs no migration.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 4 approves **250** as the next controlled stage and states the long-term goal is no practical user-visible ceiling with an internal emergency circuit breaker retained — so **the product must not be architected around 250 as a permanent maximum**. Measured rather than assumed: `predictor_internal.operating_limits.public_user_limit` already permits up to 250 by its own check constraint and by `set_operating_limits`'s validation, and stands at **50** on hosted Development. What remains is therefore one `service_role` call and the load evidence, executed as a hosted operations action — not a repository change, and not something ADR 0028 authorises by itself. |
| `CAP-007` | The global league ceiling should count active leagues rather than lifetime rows; 1,000 active leagues is a recommendation | Owner approval | Approved figures, an additive migration, pgTAP coverage and hosted verification | **Owner-approved 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 5 approves **1,000 ACTIVE leagues**, with completed league-seasons archived and no longer counting against the circuit breaker. It also states what archiving may not do: **authoritative historical prediction, settlement and result evidence is not deleted to reduce the count.** What remains is a league-season lifecycle with a completed/archived state, invite capability expiring with it, a persisted compact final-member summary (at minimum final position, final points and field size), active-count arithmetic that excludes archived leagues, and history/H2H/Wrapped still reading through. |

## Domestic table configuration

Authority: [ADR 0012](../adr/0012-season-predictor-rules.md) and contract 160, which built
`season_table_rules`, `season_table_adjustments` and `season_fixture_awards` and left them
empty by design.

Measured on hosted Development, 11 August 2026: **neither league season holds a
`season_table_rules` row.** `get_competition_table` therefore falls back to its built-in
`goal_difference, goals_for, head_to_head_points` ordering and reports **no** promotion,
playoff or relegation boundary at all, because those have no default and none was invented.

Setting them is an **operator action, not a migration** — the same reasoning contract 127
used for opening a season: `admin_set_competition_table_rules` exists, is granted, and takes
the values as arguments precisely so that a competition's published rules are data rather
than schema. What is missing is the decision about what those values are, and two of the four
are not this repository's to assume.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `TABLE-001` | The Premier League 2026/27 and Scottish Premiership 2026/27 seasons carry their own `season_table_rules`: points values, ordered tie-break keys, and promotion/playoff/relegation boundaries | Contract 160 (delivered); an owner confirmation of the two uncertain values below | Two `admin_set_competition_table_rules` calls executed against a target, and `get_competition_table` returning a boundary-annotated table for each | **Owner-decided 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 10 answers both of the two values this row deliberately refused to guess, by naming the source rather than the numbers: the configuration must reproduce the **current official governing competition rules**, verified against primary governing-body sources for 2026/27, and not Predictor-specific invented tie-breakers. **Where the stored vocabulary cannot represent an official rule** — the Premier League resolving a remaining tie by playoff, for which contract 160 has no "same position" key — the implementation must model or explicitly preserve that official behaviour rather than silently substituting a different key such as head-to-head. **No placeholder deduction and no awarded outcome may be seeded**, which is unchanged. |

## Last Man Standing organiser powers

Authority: [ADR 0013](../adr/0013-last-man-standing-season-rules.md), [ADR 0023](../adr/0023-hub-information-architecture.md)
and contract 165, audited 11 August 2026.

Contract 165 gave a private organiser the reads they had none of, and deliberately **no
command**. The audit that supported it is re-run here and reaches the same answer: searched
across ADR 0013, ADR 0023, `MIG-UI-05`, `MIG-UI-06` and this register, **no accepted authority
anywhere grants an organiser the power to act for another entrant.** The nearest thing that
exists is `admin_disqualify_competition_game_entry`, which is a competition-ADMINISTRATOR
command gated on `require_competition_admin` — a platform role, not a competition's owner.

That gap is not an oversight to be filled by an implementer. In Last Man Standing the power to
submit or change another entrant's pick is the power to eliminate them, and a schema that
grants it has decided a product question by writing a function.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `LMS-001` | Whether a private Last Man Standing organiser may act for a MANAGED entrant — one who has no account and plays through the organiser — and if so, which actions, before which deadline, and with what audit | An owner decision amending ADR 0013; none exists | The rule, then separate audited commands carrying actor and subject identities, managed-entrant eligibility, deadline enforcement from database time, idempotency, immutable audit evidence, no ability to act for an ordinary self-managed player, and no post-lock mutation | **Owner-decided 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 9 answers it **yes, narrowly**: a private Last Man Standing organiser may submit or change a pick for an entrant **explicitly marked as managed and holding no self-managed account**, before the same authoritative deadline that governs a normal entrant, with actor and subject identities stored distinctly, fully auditable and idempotent where applicable. The organiser may **never** act for an ordinary self-managed player, and **no organiser path may mutate a pick after lock**. The status text that stood here said the answer might be no; it is superseded rather than deleted, because the shape of what was asked is what made the answer precise. |

## Predictor Championship season qualification

Authority: [ADR 0014](../adr/0014-predictor-cup-season-formats.md) and [`../predictor-cup-rules.md`](../predictor-cup-rules.md) § 6–7.

Contract 166 draws a multi-group season Championship and contract 167 reads it, so a hundred-entrant field can now start. It cannot finish. `admin_finalise_predictor_cup_groups` is the only qualification, seeding and bracket authority, and it is tournament-shaped in ways that are not incidental: it requires exactly three settled windows, and § 6.2's automatic/wildcard table covers **group sizes 3 and 4 only**. ADR 0014 caps a season group at **twenty** and says "then qualification, seeded playoff and knockout" without saying how many of those twenty go through.

**This is a product decision and a migration is the wrong place to take it.** § 6.2's two rows are consistent with several incompatible generalisations — top half automatic, a fixed two per group, a proportion tuned to the § 6.1 two-thirds target — and they give materially different competitions. Picking one silently would set a Championship's shape by implementation accident, which is the failure ADR 0014's own "letting the creator choose the structure" rejection exists to avoid.

Everything else in the path is already determined for any group size and is **not** blocked by this row: § 6.1's target, § 6.3's per-game wildcard ranking, § 7.1's seeding bands, § 7.2's playoff/bye arithmetic and § 7.3's same-group avoidance.

### A tie-settlement driver is not the missing piece, and building one would create a defect

Recorded 11 August 2026 after measuring, because the obvious next step here is the wrong one and
the next session will reach for it.

`predictor_internal.settle_season_cup_tie` (contract 74, corrected at contract 96) is a pure
`jsonb` rule function that decides one Championship tie. **It has never had a caller anywhere in
this repository** — contract 96's own header says so and gives that as the reason its drift with
`cupTieSettlement.ts` went unnoticed for two contracts. The apparent gap is therefore an
orchestration driver that reads a group's fixtures, calls it, and stores the outcome.

**That driver must not be written on those terms.** A season Championship group table is not
derived from stored tie outcomes: `predictor_internal.cup_season_group_tables` (contract 169)
computes table points, points for and points against **inline**, from
`predictor_internal.cup_window_scores` over every settled group fixture, and that function is what
`get_season_cup_phase` and `get_season_cup_group_stage` return to a browser. Writing tie outcomes
into `bonus_cup_fixtures.winner_user_id` for a group stage would add a **second expression of one
ranking rule**, free to disagree with the first — the shape contract 150 refused snapshots for and
contract 169 was written to correct. Group fixtures are also the only Championship fixtures nothing
settles today: `admin_settle_predictor_cup_round` deliberately filters `stage <> 'group'`.

So the honest position is that **`settle_season_cup_tie` and `cup_season_group_tables` are two
implementations of ADR 0014's group-stage result rule, and only one of them can be the authority.**
Which is an owner decision and is `CUP-005` below. Nothing is blocked on it today, because the
derived table is the one in use and it is correct; what is blocked is anyone writing the driver
without first retiring the other.

The KNOCKOUT half needs no such decision and is already neutral: `admin_settle_predictor_cup_round`,
`cup_window_scores`, `cup_window_settled` and the Penalty Number target were all generalised to
competition-season scope by contracts 75 to 77 and 98. A season knockout tie would settle through
the delivered path unchanged — **there is simply no knockout stage to settle**, because
`admin_finalise_predictor_cup_groups` cannot qualify anyone until `CUP-001`.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `CUP-001` | An accepted rule for how many entrants qualify automatically from a season Championship group of 5 to 20, and which ranks form the wildcard pool | An owner decision amending [`../predictor-cup-rules.md`](../predictor-cup-rules.md) § 6.2, or an ADR 0014 amendment | The amended § 6.2 table covering every group size ADR 0014 permits, with the § 6.1 target reconciled against it at the field sizes ADR 0014 names | **Implemented 12 August 2026.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 6 authorised the design and deliberately did not invent the numbers; they are in [`../predictor-cup-rules.md`](../predictor-cup-rules.md) § 6.2, stated as **arithmetic** rather than a list so that no size can be missing: automatic places `floor(N/2)`, qualifying limit `ceil(2N/3)`, wildcard pool between them. **It reproduces the tournament's own 3- and 4-player rows exactly**, which is the property that makes it an extension of the accepted rule rather than a replacement, and both are asserted. Two properties are asserted at **every** size 3 to 20 rather than reasoned about once — automatic places never overshoot the § 6.1 line, and at least one entrant is always eliminated — together with a partition check that every rank falls in exactly one band. The defect it closes was concrete: `admin_finalise_predictor_cup_groups` generalised everywhere except two expressions, so a group of 5 to 20 qualified only its winner and had **nobody** wildcard-eligible. **It creates no driver.** The finalisation gate still requires the tournament's three settled windows and the knockout windows a season has none of, which is `CUP-002`. **Applied to no hosted environment yet.** |
| `CUP-002` | A season qualification, seeding and bracket driver: the § 6.1 target, § 6.3 per-game wildcard ranking, § 7.1 bands, § 7.2 arithmetic and § 7.3 avoidance, gated on the group stage the competition actually plays rather than on three windows, and reading contract 169's season table rather than the tournament's | `CUP-001`; contract 169's `cup_season_group_tables`; contracts 166 and 167 | A driver plus pgTAP proving a season group stage of N matchdays qualifies, seeds and brackets, and that the tournament path is untouched | **Implemented 12 August 2026 as contracts 186 and 187.** Contract 186 stores the group-stage span at launch; contract 187 generalises `admin_finalise_predictor_cup_groups` onto it and onto contract 169's dispatcher, appends the knockout windows through contract 110's unchanged generator, and adds `public.admin_finalise_season_cup_groups` as its granted competition-administrator caller. **Four things this row said were not quite right, corrected rather than worked around.** (1) There are **four** hard-coded window expressions, not three: the settle gate, the existence check and **two** `sequence = 4` lookups — plus four reads of `cup_final_group_tables`, whose own § 5.2 keys are measured over sequences 1 to 3, which no amount of window generalising would have fixed. (2) The span is **not** stored nowhere: both launchers already wrote it to `bonus_competition_audit`, as `leagueRounds` and as `roundsNeeded`. It was evidence rather than an authority — untyped `jsonb`, append-only log, two spellings, no constraint — so the typed record was still needed, and because the plan really was recorded the backfill is an **exact read** rather than a reconstruction. (3) Neither season launcher ever set `bonus_competitions.draw_completed_at`, so the gate refused every season Championship on its **first** check, before any span expression ran; both now record their own draw. (4) `admin_finalise_predictor_cup_groups` is granted to `service_role` and nothing else, so the driver had no caller a competition administrator could reach. **It generalises in place rather than adding beside**, the opposite of contract 169 and for the opposite reason: a second driver would duplicate about a hundred and fifty lines of ADR 0014. The tournament is unchanged **by construction** — `cup_group_stage_span` returns exactly 3 for one, contract 169's dispatcher returns `cup_final_group_tables`'s own rows, and the window generator returns immediately — and `235_season_cup_qualification_driver.sql` drives a real eight-entrant Championship over a fourteen-matchday group stage: with the tournament's three windows settled the gate **refuses**, naming fourteen. **Applied to no hosted environment yet.** |
| `CUP-006` | Whether `select_season_cup_format` must reserve calendar for the knockout it implies, rather than reporting whatever rounds happen to be left over | ADR 0014, or an amendment to it; contract 187 | The format's own `tail.rounds` reconciled against § 7.2's bracket arithmetic at every field size ADR 0014 permits, and a launcher that cannot start a competition it could not finish | **Owner-decided 18 August 2026 and IMPLEMENTED by contract 198.** ADR 0028 § 20 records the decision and corrects ADR 0014's worked table, which reported what was LEFT OVER rather than what a bracket NEEDS. **A knockout is what happens when the field is too big for one league.** A single group is a league and finishes as one; the league is never shortened to make room, and a knockout is added only when the rounds left after it can hold the whole bracket the qualifier count implies. A multi-group field cannot be one league, so it always ends in a knockout and the calendar is reserved by arithmetic working backwards before the groups are sized — sixty entrants over 38 matchweeks became four groups of fifteen rather than three of twenty. The contradiction with contract 186 is resolved in contract 186's favour. `predictor_internal.cup_knockout_rounds` is extracted from `admin_finalise_predictor_cup_groups`, which now READS it, so the launcher that reserves and the gate that creates the windows share one answer. The shortfall no longer surfaces at qualification time months after the group stage was played. |
| `CUP-003` | A read for the guaranteed Penalty Number decider and the bracket a season entrant is in | `CUP-002` | A granted, bounded read plus pgTAP, reusing `submit_cup_penalty_number`'s existing write | **Implemented — contract 193 (`get_season_cup_bracket`), 17 August 2026.** The row's own description needed one correction, made by measuring the installed catalogue rather than the migration text: `submit_cup_penalty_number` is **already season-capable**, because contract 98 replaced its `bonus_window_fixtures`/`matches` join with the competition-neutral `cup_window_first_kickoff`. The write authority is reused unchanged, exactly as this row requires. What was genuinely absent is the read: `get_season_cup_player_view` (contract 133) is group-scoped throughout and has no knockout branch, so a qualifier had no read of their own tie. The new read returns the live tie, Penalty Number state, progression, bracket and champion, and **recomputes no seat, round count or pairing** — every structural fact comes from `bonus_cup_fixtures` and contract 186's `bonus_cup_launches`. **The opponent's Penalty Number is never returned, and neither is whether they submitted**; the migration guards that the table is reached exactly once and only with `pn.user_id = v_uid`, and both guards are mutation-tested. **No consumer yet** |
| `CUP-004` | Walkover, withdrawal and disqualification handling for a Championship tie | An accepted rule; none exists | The rule, then a driver and its pgTAP | **Implemented — contract 194, 17 August 2026.** The gap was larger than "unimplemented" suggests: measured over the installed `admin_settle_predictor_cup_round`, `game_memberships`, `disqualif`, `withdraw` and `left_at` each appear **zero** times, so a disqualified or withdrawn entrant who had submitted before being removed **still won their tie on points and advanced**. `predictor_internal.cup_entrant_eligibility` reads `game_memberships.status` and reports `eligible` for an absent row, because a gap in the data must not decide a knockout tie. The eligibility branch sits ABOVE the submission ladder and that ladder is byte-identical to contract 98's, **proved by round trip** rather than by inspection — ADR 0022 forbids altering the "neither submitted" rule and one function serves the tournament as well. ADR 0028 § 8's four clauses land respectively on the branch, `bonus_competition_audit`, the absence of any score or points write, and a whole-window refusal when neither entrant may contest. `decided_by` stays inside its existing vocabulary because the ADR names audit evidence as where the reason belongs. **No consumer yet** |
| `CUP-005` | Which of the two implementations of ADR 0014's group-stage result rule is the authority: `predictor_internal.settle_season_cup_tie`, which stores an outcome per tie and has never had a caller, or `predictor_internal.cup_season_group_tables`, which derives the same result inline and is what a browser reads | An owner decision; see the note above | One authority, the other retired or reduced to its consumer, and a test that they cannot disagree | **Implemented 12 August 2026.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 7 names `predictor_internal.cup_season_group_tables` the sole season Championship group-stage authority, and it now says so on itself. **Measured on the installed catalogue, `settle_season_cup_tie` is reached by nothing**, so the two could not disagree even before this — the row described a future risk. It is narrowed by declaration plus `assert_single_group_stage_authority()` rather than dropped: it settles one head-to-head tie rather than deriving a group table, which is the knockout rule `CUP-002` needs; it carries the differential parity control against `src/domain/season/cupTieSettlement.ts`; and dropping it would be destructive. The guard refuses the **conjunction** — reaching the tie rule and writing the group stage — because a blanket ban would be deleted by whoever writes the knockout driver. The suite installs that offending shape and requires the guard to fire and to name it. **Applied to no hosted environment yet.** |

## UI finalisation backend queue

Authority: [owner UI finalisation direction, 10 August 2026](../design/ui-finalisation.md), § 12. Detailed presentation intent for each consumer is in that document.

These are the backend capabilities the finalised signed-in product needs and the weekly season does not currently expose. `MIG-UI-08`–`MIG-UI-12` were added by the direction's 10 August amendment and by the frontend work that reached their boundaries. **They are a separate backend workstream.** A UI session that reaches one of them records the data requirement here and continues with other UI work; it does not create a speculative migration and does not fake the behaviour in the client. Identifiers are the direction's own, so the two cannot drift apart.

**Eleven of these rows now have their backend implemented, and the rows say so in place rather than being deleted.** Contracts 147–151 landed on 10 August 2026 and the frontend consumed them on 11 August: `MIG-UI-12` by contract 147, `MIG-UI-11` by 148, `MIG-UI-01` by 149, `MIG-UI-03` by 150 and `MIG-UI-02` by 151. **They must not be described as blockers anywhere from that date**, and a document that still does is stale rather than authoritative. The six that remained — `MIG-UI-05`, `-06`, `-07`, `-08`, `-09` and `-10` — had their backend landed on 11 August 2026 by contracts 152–157. **Those six rows still read "Accepted — unimplemented" until 11 August 2026**, when this session found them: the batch's own note said the backend had landed and the rows it referred to said the opposite, which is the exact failure this register exists to prevent. They are marked in place below, each stating that the backend exists and the FRONTEND has not consumed it — a different fact from the backend existing. `MIG-UI-13` was closed the same day by contract 160. `MIG-UI-04` is optional and was never a blocker.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `MIG-UI-01` | A season league-wide prediction reveal read: for one private league and one matchweek after that matchweek's authoritative lock, each member's user id, display name, whether the row is the caller, their prediction per fixture, Joker state, points once settled, and member/prediction counts | Contract 128's league membership, contract 114's card facts and the season lock resolver; the weekly equivalent of the tournament's `get_league_match_picks` | A granted, bounded read plus pgTAP proving a non-member is refused and that nothing is returned before that matchweek's lock, with no client-supplied time | **Implemented** — contract 149 (`get_season_league_matchweek_predictions`), with its pgTAP, and consumed by the frontend on 11 August 2026: the Match Centre's "Your leagues" section, the private-league workspace's Matchweek tab as a desktop matrix and a purpose-built phone layout, and every "see league predictions" journey. The reveal decision is the read's `revealed`; nothing in the browser reads a clock, and before the lock no member rows and no predicted count are shown |
| `MIG-UI-02` | A season player profile / prediction-history read: season points, rank, matchweeks played, exact-score and correct-outcome counts, accuracy, recent matchweek points, best matchweek, Joker summary and revealed prediction history for one player | `MIG-UI-01`'s reveal rule; a decided disclosure boundary — recommended first release is that identity is visible to private-league co-members and prediction detail only after that matchweek locks, with no public player directory | A granted read plus pgTAP for the co-membership and per-matchweek reveal boundaries, and a `/player/:id`-equivalent route consuming it | **Implemented** — contract 151 (`get_season_player_profile`) took the recommended boundary unvaried, and the route consuming it is `/competitions/:competitionSlug/:seasonSlug/players/:playerId`, registered 11 August 2026. It is competition-scoped rather than `/player/:id` because points, rank and prediction history are facts about a player IN a season, and a platform-level address would have to pick one or invent a combined total no authority computes. The refusal is rendered as a refusal rather than as an empty profile, and there is no directory: every link comes from a surface that already names the player. Best matchweek is derivable from the history the read returns and is not separately claimed |
| `MIG-UI-03` | A season league rank/movement read: position before and after a settled matchweek, and the change in gap to the leader or a named rival | Settled matchweek scores and league membership only — derive first, and introduce stored snapshots only if derivation proves inadequate | A page-oriented read plus tests that a movement is reported only from settled matchweeks | **Implemented** — contract 150 (`get_season_league_rank_movement`), derived and never stored, and consumed on 11 August 2026 by the private-league workspace, the Match Centre's league section and the Hub's Matchweek Recap. `settled` is the gate in the read and again in the decoder, so an unsettled matchweek produces no movement anywhere; ordering is by round ordinal, so a postponed matchweek settling late does not appear to come after ones played after it |
| `MIG-UI-04` | Multi-matchweek head-to-head: last five matchweeks against one rival, cumulative rivalry record, matchweeks won/lost/drawn | Contract 129's one-matchweek head-to-head | A read plus tests, reusing contract 129's reveal boundary | **Implemented** — contract 192 (`get_season_rivalry`), which answers the whole comparison in ONE request rather than the thirty-eight calls contract 129 would have taken: both identities, rank, points, matchweeks played, exact-score and correct-outcome counts, the head-to-head matchweek record, recent form clamped to 20 and the signed points gap. Contract 129's reveal boundary is reused rather than restated, including its choice to hide on a null lock, and no individual prediction is returned. **A matchweek only one player banked does not count**, so a late joiner cannot be handed a walkover record, and `matchweeksCompared` travels beside the record. **No consumer yet** |
| `MIG-UI-05` | Private Last Man Standing creation, invite and join, under the same contract shape as the private Match Predictor league | Existing container/join authorities; server-owned creator limits and game rules | Create, invite and join journeys for a private LMS competition with no client-side rule duplication | **Implemented** — contract 153 (`create_private_season_lms`, `join_private_competition`), and consumed by the frontend on 11 August 2026: `CreatePrivateJourney` offers Last Man Standing beside the other two games, with the four rules ADR 0013 makes the organiser's — lives, saves, draw treatment, wipeout answer — offered as the ranges `season_lms_setups` constrains and never re-validated in the browser. The calendar is deliberately not offered, because it comes from the same generator the public competition uses. `no_schedulable_round` is surfaced as its own outcome rather than dressed as success **Consumed by the frontend on 11 August 2026**: `CreatePrivateJourney` offers Last Man Standing beside the other two games, with lives, saves, draw treatment and the wipeout answer offered as the ranges `season_lms_setups` constrains and never re-validated in the browser, and `no_schedulable_round` surfaced as its own outcome rather than dressed as success |
| `MIG-UI-06` | Private Predictor Championship creation, invite, join, owner configuration, entrant setup and deterministic format creation | Contract 111's launch driver and contract 124's phase transition | Create, invite and join journeys for a private Championship, with the format fixed by the server rather than described by the client | **Implemented** — contract 154 (`create_private_season_cup`, `launch_private_season_cup`), consumed 11 August 2026. Creation and launch are two acts and the interface never blurs them: the created step carries the invite code and the launch control, and the sentence about the draw being fixed at the field size found — and registration closing with it — comes before the button rather than after it. `no_viable_format` invites the organiser to gather more entrants and try again; contract 111 keeps the format, the seeding and the draw entirely **Consumed by the frontend on 11 August 2026**: the same journey creates a Championship and then launches it, with the sentence about the draw being fixed at the field size found — and registration closing with it — placed before the control rather than after it |
| `MIG-UI-07` | A universal invite-code resolver: one code entry point that resolves competition, season, game, private container, eligibility and any required underlying game membership | Audited and **partially closed without a migration**; see the note below | The remaining gap is a code for a container that is not a league. Evidence would be one resolver that answers for a private Last Man Standing or Championship as well, or those containers existing at all (`MIG-UI-05`, `MIG-UI-06`) | **Implemented** — contract 155 (`resolve_invite_code`), consumed 11 August 2026. `/join/:code` and the code-entry sheet both resolve first and join second, and the SERVER's `joinWith` decides which join is called — never the code's shape. A Last Man Standing or Championship link now works where it was previously answered "that code doesn't match a league". Unknown, malformed and rotated codes share one sentence, and the member count the resolver returns is deliberately **not rendered**, because contract 158 removed exactly that disclosure from `get_league_preview` under `SEC-001` and a second surface showing it would reopen the hole **Consumed by the frontend on 11 August 2026** on both `/join/:code` and the code sheet, with the server's `joinWith` deciding which join is called; the frontend decoder was narrowed to match contract 159 the same day and now has no `id` or `members` field to render |
| `MIG-UI-08` | A season history / Wrapped archive contract: an idempotent, immutable end-of-season snapshot generated from canonical settled data — final points, rank, field size, percentile, rank journey, best/worst rank, biggest climb, best matchweek, exact/correct totals, streaks, Joker summary, club outcomes, private-league finishes, LMS and Championship outcomes, trophies | Authoritative season completion and settlement; `MIG-UI-02` for the per-player aggregate | A snapshot/read contract plus tests that it can only finalise after completion, that creation is idempotent, and that a stored Wrapped does not change when a later UI formula does | **Implemented** — contract 156 (`get_season_wrapped`), consumed 11 August 2026 by the platform profile's Season history section. Every figure is one the archive stored and nothing is recomputed: no derived percentile, no career total, no trophy count, because the archive holds none of them. A season still being played is listed and says so rather than being rendered as zeros. The one thing it could not do on its own — find a Wrapped for a season no longer published — was `MIG-UI-17`, and that row closed later the same day: the section now reaches the archive through contract 161's participation-keyed index, so the copy that used to state the limit is gone with the limit **Consumed by the frontend on 11 August 2026** as the platform profile's Season history, which renders only what the archive stored and recomputes nothing |
| `MIG-UI-09` | A pinned-rival preference: one or two private-league co-member ids in a competition/season context, so Rival Watch survives a device change | An audit of the existing account/preferences authority first — the narrowest possible contract if it cannot hold them | Either the audit concluding no change is required, or a preference contract with tests. Never followers, friend requests, public social graphs or user discovery | **Implemented** — contract 157 (`set_pinned_rival`), consumed 11 August 2026 as the Hub's Rival Watch. The picker IS a private league table the player is already in, which is exactly the boundary the server enforces, so there is no search and no directory. The comparison subtracts two numbers already in `get_league_members`' answer and is not a second scoring authority; rank is labelled as the LEAGUE's, because contract 128 recomputes it inside the league **Consumed by the frontend on 11 August 2026** as the Hub's Rival Watch: the picker is a private league table the player is already in, which is the boundary the server enforces, so there is no search and no directory |
| `MIG-UI-10` | Persistence for the non-game onboarding choices: followed competition seasons, optional favourite team, and onboarding completion/progress — keyed on canonical competition and team identity, supporting many competitions without a schema change per league | An audit of the existing profile/preference/membership authorities first. Game memberships keep their existing server authorities and must not be duplicated | Either the audit concluding an existing authority can hold them, or one persistence contract that keeps Follow distinct from game membership, allows onboarding resume and later edits, preserves pending invite context and is publication-aware | **Implemented** — contract 157 (`get_my_preferences`, `set_competition_follow`, `set_onboarding_progress`), consumed 11 August 2026. `followed` and `favourite` are no longer `'unknown'` in `playerCompetitions.ts`; Follow and game membership are unioned so a followed competition with no game joined is visible, and `'unknown'` now means only that the preference read did not land. `/welcome` runs the four-step journey, writes progress as the player moves, resumes both the step and the choices, and hands a pending invite back at the end. Account gains follow/unfollow and a favourite club per competition **Consumed by the frontend on 11 August 2026**: `followed` and `favourite` are no longer `'unknown'` in `playerCompetitions.ts`, Follow and game membership are unioned, `/welcome` runs the four-step journey with server-side resume, and Account gains follow/unfollow and a favourite club per competition |
| `MIG-UI-11` | A season fixture read addressed by fixture id, returning the one fixture with its round, kickoff, status, result and provisional score | Contract 139's fixture facts | A bounded read plus a route test that a Match Centre link resolves without a date hint | **Implemented** — contract 148 (`get_season_fixture`), returning contract 139's entry field for field so a fixture looks identical whether it arrived from the calendar or from a link. Consumed 11 August 2026: `competitionMatchCentreRoute` no longer takes a day, the `?on=` query is gone from every link, the three-week window search is deleted, and the "that match is not in this window" alert has gone with the gap it described. A provisional provider score still stays in `live` and never becomes a result |
| `MIG-UI-12` | A browser-safe read of the published weekly competition catalogue: canonical competition and season identity, competition display name, **route slug**, season label, lifecycle/publication state, and weekly-platform eligibility — so publishing a league needs no frontend code change for it to exist | Audited, then closed by contract 147; see the audit note below for what the audit found first | The remaining gap was one field. Evidence: a bounded read exposing the route slug, plus a frontend that resolves a competition's URL from it and a test that a newly published season is routable with no static-catalogue edit | **Implemented** — contract 147 (`get_published_weekly_seasons`) returns the slug beside the season key. Consumed 11 August 2026: `HUB_COMPETITIONS` is **deleted**, `catalogueFromPublishedSeasons` builds the catalogue from the read, and `tests/features/hub/serverDrivenCatalogue.test.ts` proves a newly published season is routable with no frontend edit and that no slug is derived from a name. A publication decision distinct from `tournaments.status` was NOT taken and is not claimed — the RPC excludes drafts and the browser does not re-filter |
| `MIG-UI-13` | A domestic **league table** for a competition season: each club's played, won, drawn, lost, goals for and against, goal difference and points, under the competition's own rules — including any deduction, its tie-break order and any promotion/relegation boundary it publishes | Settled `season_fixtures` results, plus wherever a competition's table rules are to be stored | A read plus tests that a deduction and the competition's stated tie-break order are applied server-side, and a Matches-section Table surface consuming it | **Implemented** — contract 160 (`get_competition_table`), and **consumed by the frontend on 11 August 2026**: the competition Matches section now carries a Table segment below the fixtures. Every number and the ORDER are the server's — the component prints `position` rather than the array index, so a competition whose stored tie-break order changes needs no frontend edit. A promotion, play-off or relegation boundary is drawn only where the competition PUBLISHES one and is never inferred from a club being bottom; a deduction is shown beside the club and in a footnote and is never re-applied, because it is already inside `points`; and a season with fixtures outstanding says how many of how many have been played rather than reading as final. Recent form stays beside it and is still labelled as form: contract 141's derivation is capped at twenty matches and knows none of the competition's rules, so the two are different reads answering different questions and neither stands in for the other |
| `MIG-UI-14` | A cross-competition **action-centre** feed: the small set of things a player has not yet seen — picks due, a matchweek settled, a private-league invite, a game consequence — each with a stable identity so it can be marked read, and a read cursor that survives a device change | Existing per-game action reads; a per-player read cursor, which nothing stores | A bounded read plus a surface, and a test that an item marked read on one device does not reappear on another | Accepted — **backend implemented by contract 162** (`get_my_actions`, `mark_actions_seen`, `dismiss_action`), reachable from a browser since the types were regenerated at 168 on 11 August 2026, and **deliberately not consumed yet**. Measured rather than assumed: the action vocabulary declares four kinds — `matchweek_predictions_due`, `lms_pick_due`, `cup_penalty_number_due` and `matchweek_settled` — and only `predictor_internal.generate_lms_pick_actions` exists, so `get_my_actions` returns Last Man Standing picks and nothing else. Switching the action centre onto it today would buy cross-device seen and dismissed state for one game and silently drop matchweek and Championship items that the derived inbox reports correctly — trading the thing a player opens it for against the thing they would not notice missing. **The interim action centre therefore stays**, and it was built so this trade never has to be made: the AppBar counts what is OUTSTANDING, a server-derived fact about what the player has not done, identical on every device and self-clearing when they act, never what is unread. It stores nothing locally, offers no dismiss and no mark-as-seen, and its copy contains neither "new" nor "unread". **What would close this row** is the two missing generators; the browser half is one call away and is not the blocker Contract 170 added the matchweek-card generator, contract 173 the settled-matchweek recap — filling the `matchweek_settled` type contract 162's own vocabulary and `expires_at` comment declared and left empty — and **contract 172 gave all of them a caller**, which none had: measured on hosted Development at contract 171, `cron.job` held seven jobs naming none of the three entry points and `player_action_items` held zero rows, so the inbox was permanently empty however correct the generators were. **What remains of this row is two of the four feed items.** The Championship action is blocked with `CUP-002` on `CUP-001`, because a season Championship has no bracket to act on. The private-league invitation action is unbuilt because no stable server-side invitation EVENT exists to derive an idempotent `action_key` from — an invite code is a standing capability rather than an event, so generating from it would either repeat for ever or need a new relation, and that is a design decision rather than a generator. **Contract 195 closes the Championship half.** `predictor_internal.generate_cup_penalty_number_actions` writes one item per contestable side of an unsettled, unlocked playoff or knockout tie, completed from a stored Penalty Number, and `process_player_action_items` calls it. Contracts 187 and 193 removed the blocker this row named — a season Championship now has a bracket, and an entrant can read their tie. Measured again on the installed catalogue at contract 195: all four deadline-shaped types (`matchweek_predictions_due`, `lms_pick_due`, `cup_penalty_number_due`, `matchweek_settled`) now have a generator; `game_consequence` and `league_invitation` have none. The private-league invitation remains blocked for the design reason above rather than for want of a generator, and `game_consequence` has no declared source event either. **Contract 196 closes the consequence half.** `predictor_internal.generate_game_consequence_actions` writes one item per entrant whose outcome is `eliminated`, `champion` or `qualified` — never `active` or `survived`, because a per-round survival written for every survivor is not news — inside a seven-day window so enabling it posts no backlog, carrying `deadline_at` null so it stays out of the reminder scheduler, and the expiry sweep gains a fourth re-read so a rescore that REVERSES an outcome closes the item that said otherwise. Driven rather than asserted: on hosted Development at contract 198 the generator wrote exactly **2** items at the first `player-action-centre-generate` tick after the apply, both `champion`, both Last Man Standing, both `deadline_at` null — 18 `active` entrants correctly produced nothing, and the Championship produced nothing because its 112 fixtures are all group stage and the Penalty Number generator needs a contestable playoff or knockout tie. **What remains of this row is therefore the browser consumer plus `league_invitation` alone**, and that one is a design decision — an invite code is a standing capability rather than an event, so there is nothing to key an idempotent `action_key` on without a new relation — not a missing generator. **Owner-decided 18 August 2026: ship without it.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 22 confirms the action centre is complete as a deadline and consequence feed under the current share-code model. `league_invitation` stays declared and unwritten because an invite code is a standing capability rather than an event: inventing a relation whose only purpose is to make an action item possible would add a second source of truth about league membership to serve a feed. The vocabulary keeps the type so a later share model that DOES produce an event has somewhere to put it. **What remains of this row is the browser consumer alone.** |
| `MIG-UI-15` | A product-analytics decision and seam: whether behavioural events are collected at all, by what processor, under what lawful basis and retention, and with what per-event schema | An owner decision, and `PRIV-007`'s data-protection review for anything that touches personal data | The decision recorded as an ADR, then an implementation with a documented event list and a test that no prediction value, league membership or free-text content is ever an event property | **Owner-approved in principle 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 14 approves privacy-conscious product analytics as a **capability** — allow-listed and versioned events only, no autocapture, no session replay by default, no prediction contents, free text, hidden league data or arbitrary DOM capture as properties, and analytics kept separate from Sentry. **It does not choose a vendor**, and processor choice, lawful basis, retention and the required privacy documentation stay governed by the existing privacy process. So the owner half of this row is answered and the processor/privacy half is not. |
| `MIG-UI-16` | A bounded read of one competition season's **clubs**: each club's canonical `teams.id`, its name and contract 136's identity facts (short code, colours), in one answer | Contract 136's club-identity reference, already joined server-side by the fixture reads | A granted read returning id, name and identity together, plus a test that a club with no fixture in the next few months is still returned | **Implemented 12 August 2026.** `get_season_clubs` returns every club in a league season with its canonical `teams.id` and contract 136's short code and colours in one answer. **It joins no fixture**, which is the whole point rather than an optimisation: the two-read join in `seasonClubs.ts` harvests identity from a fixture window, so a club that window does not cover silently loses its stored identity and falls back to a derived one. `232_season_clubs_and_neighbourhood.sql` seeds a club that plays **nobody** and requires its stored short code and colours back, and reads contract 136's own seeded reference rather than one the suite planted. Granted to `authenticated` and refusing anything that is not a league season, on contract 147's `EURO-001` reasoning. **The browser still performs the two-read join**; replacing it is the remaining frontend half. |
| `MIG-UI-17` | A per-player **Wrapped index**: which seasons this player has a finalised `season_wrapped` row for, so permanent season history does not depend on the season still being published | Contract 156's archive | A bounded read listing the caller's own finalised seasons by id and label, plus a test that a season absent from the published catalogue still appears | **Implemented** — contract 161 (`get_my_season_history`), which keys on PARTICIPATION rather than publication so an archived season stays discoverable, and **consumed by the frontend on 11 August 2026** as the platform profile's Season history. The section previously asked `get_season_wrapped` once per competition in the PUBLISHED catalogue, so a player's 2026/27 left their own history the day it was archived and the surface had to say so in its own copy; it now issues one paginated read instead of N, and the apology is deleted with the gap. An archived season is listed, marked `Archived` and deliberately **not** linked, because `inPublishedCatalogue` is the server's answer about routability and a link to an unpublished season goes nowhere. Nothing is recomputed: `final` is contract 156's immutable snapshot, absent for a season still being played — which is listed and says so rather than rendered as zeros — and distinguished in copy from a finished season whose Wrapped has not been written. A failed read is a failure, never an empty history, and the page bound is stated rather than left to read as a complete one |
| `MIG-UI-18` | A **leaderboard neighbourhood** read for a season game: the handful of entrants immediately above and below the caller's own rank, in one answer, without paging to reach them | Contract 95's `get_season_leaderboard`, which pages forward from the top with an opaque cursor and takes no offset and no anchor | A bounded read anchored on the caller's own rank returning a symmetric window around it, plus a test that a player ranked 4,000th of 5,000 reaches their neighbours in one request | **Implemented 12 August 2026.** `get_season_leaderboard_neighbourhood` returns a symmetric, clamped window around the caller's own rank in one request — the eighty that downward paging costs at rank 4,000 of 5,000 was never an implementation of this row, and the browser deliberately never did it. **It computes no rank.** Every figure comes from `predictor_internal.season_standings` and the total order is contract 95's `standing_rank, sort_name, display_name collate "C", entry_key`, copied deliberately — and the suite runs **both** reads and requires them to agree on the caller's position, because copying an `order by` is not evidence it was copied correctly and two reads that disagree about "where am I" both look sorted. A caller at the top receives fewer rows above and is told so by `atTop` rather than the array being padded with players who do not exist. Contract 95's membership boundary is reused verbatim. **No surface consumes it yet.** |
| `MIG-UI-19` | A **season share card**: a branded, explicitly-invoked image a player can share of a matchweek, a milestone or a finished season, exposing no unrevealed prediction and no league membership by default | A design decision about what a league-season card SHOWS, before any read | A renderer plus tests that it draws only from settled, already-revealed facts, and that a card built before a matchweek's lock exposes nothing | Accepted — unimplemented, and **not blocked on a contract**. Measured 11 August 2026: the share stack under `src/features/share/` renders a tournament bracket to a canvas from country flags, a predicted champion, finalists and a knockout pipeline — a league season has none of those, and `useShareModel` reads `TournamentDataProvider` and `PredictionsProvider`, neither of which a season populates. A season card is therefore a SECOND renderer over club identity rather than a variant of the existing one, and what it should show — a matchweek, a run of exact scores, a final position — is a design decision this session had no authority to take. Every fact it would need is already readable: contracts 114, 151 and 156 between them hold the matchweek, the season record and the archive. **Re-measured 12 August 2026 and deliberately left outstanding.** The text-and-link share entry point is delivered (`INNOV-007`) and covers the sharing a player can actually do today; the IMAGE card is not, and the reason is unchanged and is not a missing read. The Euro tournament-bracket canvas must not be repurposed for a domestic season — it draws country flags, a predicted champion and a knockout pipeline, none of which a league season has — so this stays blocked on the content and design decision rather than being approximated |
| `MIG-UI-20` | A **private competition workspace** read: one private Last Man Standing or Predictor Championship container addressed by its own id, returning the container's name, its game and competition, its members and each member's state in that game, so the container can open into a page of its own | Contract 153/154's private containers; contract 164's post-lock Last Man Standing social reads and contract 165's organiser reads; contract 167's Championship draw read | A granted read that resolves a container by id, refuses a caller who is not a member, and honours each game's own reveal boundary rather than restating it — plus a `/leagues` card that opens into it | **Server half implemented 12 August 2026; the page is not, and the row stays open.** `get_private_competition_workspace` resolves one private container by its own id, refuses a non-member exactly as it refuses an unknown id, and honours each game's reveal boundary rather than restating it — it shows THAT a Last Man Standing entrant has picked and never what. It ships with `get_my_private_competitions`, because the 12 August 2026 private-play investigation found something worse than a missing destination: `/leagues` rebuilds private play from `get_my_game_leagues`, which reads `public.leagues`, so a private container that was created or joined successfully **could not be found again at all** (`PPLAY-001`). **The remaining gap is the frontend**: `/leagues` consuming the discovery read for bonus-game containers, the post-join copy following an authoritative reread rather than an RPC result, and the page this row names. Until they land the `/leagues` card is still the container's only home, and it is still reached by the wrong read. **And the frontend half is blocked on something narrower than design work, measured 12 August 2026**: `src/services/supabase/database.types.meta.json` records `generatedFromContract: 178`, and neither `get_private_competition_workspace` nor `get_my_private_competitions` appears in `database.types.ts` — so `db.rpc()` on either name fails to typecheck (`TS2345`, proven against a probe file) and the page cannot be written type-safely at all. Regenerating is not a presentation-layer change and does not belong to a session that may not touch hosted state; it also cannot reach the repository's head today, because the generator derives its types from a hosted project and Development holds 180 against a repository at 183. The order is therefore: promote, regenerate the types, then build the page. |
| `MIG-UI-21` | A **stable player identity on the GLOBAL weekly-season standings**, so a row in the season leaderboard can be opened as a player rather than read as a name | Contract 95's `get_season_leaderboard` and contract 183's neighbourhood, neither of which returned an identifier; contract 151's disclosure boundary for the destination | Every row carrying a season-scoped reference and a server-decided reach, pgTAP proving two identically named entrants do not collide and that the auth identifier is withheld where no profile will answer, and a bounded resolver that refuses rather than answering emptily | **Implemented** — contract 191 (`playerRef`, `reach`, `playerId` on both season standings reads, plus `public.resolve_season_player`). The reference is `entries.id`: canonical, because contract 94 already ranks on it; season-scoped, by `entries unique (user_id, tournament_id)`; and not an authentication identifier, so it cannot correlate a player across two competitions. `playerId` is emitted only where `reach` is `self` or `profile`, which is exactly the set that could already read it from contract 128. **No consumer yet** — the frontend decoder carries the three fields and `PlayerReach` is modelled, but no surface links a global row, and `resolve_season_player` is absent from the generated types until Development holds 191 |
| `MIG-UI-22` | **True rank over time for a competition season**: the caller's or a permitted player's authoritative position, points and field size at every settled matchweek, using the same total order as the canonical season standings | Contract 94's `season_standings`; contract 191's visibility authority for a player other than the caller | A bounded read plus a DIFFERENTIAL proving it agrees with the canonical standings, over a fixture that actually contains ties | **Implemented** — contract 192 (`predictor_internal.season_rank_history`, `get_season_rank_history`). Points over time was already derivable from `season_matchweek_scores`; POSITION over time was not, because contract 150's movement read is one matchweek inside one private league and contract 48's rank history is the tournament's. The derivation reuses contract 94's `rank()` and contract 94's whole field, and the agreement is executed rather than asserted: at the last settled matchweek the two authorities must agree entry by entry, and the fixture is built with deliberate collisions so that `dense_rank` (10 disagreements) and `row_number` (6) both fail it. **A late joiner is in the field from the first matchweek on zero**, which is the canonical field's own behaviour and is recorded rather than corrected, because excluding them would be a second interpretation of who is in the table. **No consumer yet** |
| `PROF-001` | Whether a participant in a competition season may view another participant's settled, reveal-safe **profile** when the two share no private league | An owner/product decision; contract 191 implements the boundary as it stands and takes no position | The decision recorded in an ADR or an amendment, then one change to `predictor_internal.season_player_reach` — which is now the only place the rule lives — plus pgTAP for whichever answer is taken | **Recorded, not taken — 17 August 2026.** Today the answer is **no**, and it is an accepted decision rather than an oversight: `MIG-UI-02` named a recommended boundary and contract 151 took it unvaried. **The exact decision required, in one sentence:** does entering a competition season constitute consent to have your season profile — points, rank, matchweeks played, accuracy counts, Joker summary and REVEALED prediction history — read by any other entrant in that season, as distinct from the per-matchweek comparison contract 129 already permits between any two entrants? **What is not in question**, whichever way it goes: no pre-lock prediction, no unrevealed pick, no invite-only membership, no private league detail and no personal data outside that list becomes visible, because none of those is in the profile payload. **What it is not:** it is not a player directory, a search endpoint, a follower model or a public social graph, all of which are out of scope under a separate decision. The vNext Profiles/H2H stage can be built either way — a `compare` row is already comparable — so this is not a blocker; it decides how rich a non-league-mate's profile link is **Owner-decided 18 August 2026: YES.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 21 permits a season entrant to read another entrant's settled, reveal-safe profile in that same season. **Recorded explicitly as a product judgement, NOT a legal consent claim** — no implementer may cite it as one, and any data-protection question about it belongs to `PRIV-007`'s external review. Unchanged and not weakenable: no pre-lock prediction or unrevealed pick becomes visible, no private-league membership or invite code becomes discoverable, nothing crosses seasons, and no cross-competition public identity is created. The boundary is the SEASON — a player who never entered it reads nothing. The backend change is a widening of contract 151's visibility rule and is not yet implemented. |

### `MIG-UI-07` audit — the league half needs no migration

Measured rather than assumed, against `20260803070000_c1b_game_catalogue_memberships.sql`:

- `public.join_league(p_code text)` looks the league up **by its invite code alone**, reads `game_competition_id` off the row it found, and refuses a caller with no active membership in that game. The browser therefore never has to know — and never tells the server — which game a code belongs to. That is the "no client-side guessing at the container type" property the requirement asks for, already held.
- `public.get_league_preview(p_code text)` gives the same code a pre-join summary, so a player sees the league's name, size and owner before joining rather than after.
- `public.create_game_league` refuses any game that does not require a prediction entry, so every row in `public.leagues` belongs to a predictor game by construction. A league code cannot currently name a Last Man Standing or Championship container because no such container exists.

**What remains** is therefore not a resolver but the containers: once `MIG-UI-05` or `MIG-UI-06` creates one, a code for it needs somewhere to resolve to, and whether `join_league` widens or a new resolver appears is that work's decision rather than this one's. Until then a single code entry point is truthful today and is shipped as one.

### `MIG-UI-12` audit — what already exists, and the one thing that does not

Carried out before proposing anything, as the requirement asks. Findings, all measured against the migrations in this repository rather than assumed:

| Question | Answer today |
| --- | --- |
| Canonical season identity | **Yes** — `public.tournaments.id`, and `tournaments` is readable to `authenticated` (`tournaments readable`, select `true`) |
| Display name | **Partly** — `tournaments.name` is the combined "Premier League 2026/27". The competition's own name lives in `public.competitions.name` |
| Season label | **Yes** — `tournaments.season_key` |
| Lifecycle state | **Yes** — `tournaments.status` (`draft`/`scheduled`/`active`/`complete`/`archived`) |
| Weekly-platform eligibility | **Yes, canonically** — `tournaments.kind = 'league_season'`. Euro is `kind = 'tournament'` and is excluded by its own stored kind, not by an allowlist; its `euro_publication_state()` boundary is untouched |
| Available games | **Yes, per season** — `get_competition_games(uuid)`, which describes a season and cannot enumerate one |
| Calendar zone | **Yes** — `tournaments.display_timezone` |
| **Route slug** | **No** — `public.competitions.slug` is the canonical route identity and `public.competitions` is `revoke all … from public, anon, authenticated` |
| Publication decision distinct from lifecycle | **No** — `status` is a lifecycle. The weekly platform has no equivalent of Euro's publication state |
| Grouping metadata (region, country, type, popularity) | **None held.** Not invented; Explore stays search + pinned + the rest |

**What was therefore implemented in the frontend, with no migration:** `fetchPublishedSeasons()` enumerates `tournaments` where `kind = 'league_season'`, and `presentPlayerCompetitions` treats that as the catalogue. `HUB_COMPETITIONS` is demoted to **transitional presentation metadata** — a route slug and copy for the seasons it happens to know — and a published season it does not know is listed in Explore under "Newly published", named with its lifecycle and explicitly not openable, rather than being invisible. `tests/features/hub/serverDrivenCatalogue.test.ts` pins that boundary.

**What still needed the backend, and what happened to it:** the route slug — closed by contract 147 on 10 August 2026 and consumed on 11 August. `fetchPublishedWeeklySeasons()` replaced the table read, `HUB_COMPETITIONS` was deleted, and the frontend still never derives a slug from a name: that would be a client-side invention of a server-owned identifier, and the season routes were built specifically to avoid it.

**What was deliberately NOT taken:** a publication decision distinct from `tournaments.status`. The catalogue read excludes drafts server-side and the browser does not re-filter, so Production — where both league seasons are draft — correctly returns nothing. Whether the weekly platform should gain its own publication state, as Euro has under contract 143, remains an owner decision and not a frontend one.

## Innovation Lab backend

Authority: [ADR 0027](../adr/0027-innovation-lab-backend-foundations.md), accepted 12 August 2026. Candidate detail: [`../product/innovation-lab.md`](../product/innovation-lab.md), which decides nothing and is not an authority.

The `INNOV-*` identifiers are the register's own and are permanent, so they are reused here rather than re-issued: a candidate that is promoted keeps the number it was discussed under. A row's presence here means the owner accepted a **backend direction**, not that the product feature is approved for release.

**Four of these rows are marked implemented.** They are kept rather than deleted, as this register requires: deleting a row destroys the only trace that the requirement existed and was met.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `INNOV-001` | A read-only projection of one matchweek under the current or a hypothetical score, reusing the ADR 0012 scoring authority, labelled provisional where a provider score is its basis, and revealing a rival only after that matchweek's own lock inside a shared private league | ADR 0012 scoring; contract 135 live state; contract 149's reveal boundary | An RPC plus a pgTAP suite proving the projection equals `season_matchweek_points` over a fully played matchweek and hides the league block before the lock | **Implemented — contract 175, and consumed 12 August 2026.** `get_season_matchweek_projection`, 23 assertions in `224_what_if_projection.sql`, two mutants killed. The Match Centre's What-If panel now reads it instead of projecting in the browser: the fold over the league's revealed predictions, the client-side ranking and the banked-points read are deleted. Each branch shows the WHOLE matchweek under that hypothetical, post-Joker, and the basis is the server's word — `official`, `provisional` or `none` — never inferred from a score being present. The league block is the server's, before the lock hidden completely, and it is labelled as the projection AS THE MATCHWEEK STANDS, because contract 175 does not project a league under a branch and the browser does not rebuild one |
| `INNOV-002` | Deterministic per-player prediction metrics with every denominator returned and an explicit minimum sample, over matchweeks whose own lock has passed, under contract 151's disclosure boundary | Contract 151; contract 61's cohort minimum | An RPC plus a pgTAP suite proving the window excludes an unlocked matchweek and the cohort excludes a small field | **Implemented — contract 176, and consumed 12 August 2026.** `get_season_prediction_dna`, 24 assertions in `225_prediction_dna.sql`. A player's season page reads it and the browser derivation is gone with the defect it carried: the old exact-score rate came from a different denominator than the tendency shares, so two percentages sat side by side over different populations. The panel is three groups with three stated denominators — locked predictions, settled predictions, comparable fixtures — and the server's own `sufficient` flag is obeyed: a short group shows its counts and states the shortfall, and the signature word (with it the share control) is withheld. A null rate renders no metric rather than a 0%. Per-club tendency and agreement with the field exist at all, which contract 151's history could not supply |
| `INNOV-003` | A deterministic fact layer for a natural-language analyst, and — separately — a server-only model integration with a strict tool allow-list and no database authority | `INNOV-002`, contracts 150, 151 and 129 for the facts; an owner decision on an AI provider and a privacy review before any personal context leaves the platform | The fact layer: reads answering rank movement, comparison and divergence. The model half: an Edge Function holding no client-reachable credential, with a fixed tool allow-list and structured evidence returned beside prose | **Architecture approved 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 15 approves a server-side-only explanation layer over deterministic facts: credentials server-side, a strict tool allow-list, no arbitrary SQL or database authority, no ability to write predictions, results, scoring, membership or admin state, and deterministic reads remaining the authority for every competitive fact. **No model provider is chosen**, and real-user data may reach one only through the existing processor review. The deterministic fact layer can proceed now; the model half cannot start from this row alone. |
| `INNOV-004` | Public spectator league pages behind an explicit, revocable, owner-controlled visibility opt-in with a versioned public field allow-list | An owner decision on the visibility model and the field allow-list; `INNOV-007` and `INNOV-008` share it | A `visibility` model on the private container, an anon-executable bounded read, and anon-role pgTAP proving a private league is unreachable and a public one exposes no address, auth identifier or pre-reveal prediction | **Owner-approved 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 11 approves the public-visibility family: private stays the default, a league owner may opt an eligible league into a **revocable** public spectator mode, and the first allow-list may contain only league display name, competition/game identity, player display names within that public league, standings and settled movement/honours, already-revealed prediction/result aggregates, fixtures and the next relevant lock. It must expose no address, auth identifier, private profile field, invite secret or unrevealed prediction. **`INNOV-004`, `INNOV-007` and `INNOV-008` must reuse ONE visibility authority**, not three. Anon-role pgTAP remains the acceptance evidence. |
| `INNOV-005` | A server-owned single-fixture challenge lifecycle: unguessable guest token, hidden challenger pick until the fixture's lock, explicit expiry and retention, rate limiting, and an account claim that cannot hijack another guest's challenge | An owner decision on guest data retention and on whether an unauthenticated visitor may write at all | A challenge domain model plus pgTAP covering create, submit, duplicate submit, pre-lock hiding, post-lock reveal, expiry, late submission and claim | **Owner-approved 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 12 approves the guest challenge as a bounded unauthenticated acquisition path, with the challenger's pick hidden until the normal reveal, an ownership-safe account claim, and **30 days after the fixture** as the unclaimed-guest retention. A challenge must not silently join either player to a season game or a private league. Rate limiting, expiry and anti-hijack tests are required. |
| `INNOV-006` | A bounded large-screen read composing standings, live fixtures, revealed predictions and movement without N+1 browser queries | `INNOV-001`; measured evidence that composition in the browser is actually the cost | A single read, or a recorded measurement showing the existing reads are sufficient | Accepted — **backend already sufficient, pending measurement.** Contracts 128, 139, 149, 175 and 135's live state supply every fact. A composing read is an optimisation, and ADR 0027 declines to build one before a measurement shows the browser's assembly is the bottleneck |
| `INNOV-007` | A server-side share payload from a versioned public-safe allow-list, revocable where private-origin data is involved | `INNOV-004`'s visibility model | A share contract plus tests proving no pre-reveal value and no private profile field can appear | **Owner-approved 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 11 approves it as part of one public-visibility family with `INNOV-004` and `INNOV-008`. It must reuse that single allow-list rather than defining a second, and revocation must invalidate a share built from private-origin data. |
| `INNOV-008` | A deliberately narrow embed read reusing the public visibility model rather than creating a second one | `INNOV-004`'s visibility model | An embed read with origin, cache and rate-limit review | **Owner-approved 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 11 approves it as part of one public-visibility family with `INNOV-004` and `INNOV-007`, reusing that visibility authority rather than creating a second public security model. |
| `INNOV-009` | A read/notification-only messaging integration with server-held secrets, explicit league-owner setup and revocation, and an audit trail | A named consumer platform, its terms review, and the brand decision the sender identity depends on | Integration connection storage, an Edge Function delivery path, and revocation plus audit tests | **Platform target chosen 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 17 names **WhatsApp first** rather than Discord, with the first release read/notification-only — table, next deadline and completed-matchweek summaries, and no prediction writes through messaging. **It is conditional**: current WhatsApp Business/platform requirements, costs, webhook constraints, opt-in and message-template rules and privacy implications must be verified against primary Meta documentation before build, and if they make a useful first release impractical the evidence is recorded and the decision returns to the owner rather than the provider being silently switched. |
| `INNOV-010` | Server-side wallet pass generation with signing credentials in secrets, revocable device registration and an obvious stale state | Platform enrolment, signing credentials, and the brand decision | Pass generation, an update endpoint and revocation tests | **PARKED by the owner, 12 August 2026.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 18 parks this: no pass-signing infrastructure, enrolment, device registration or UI during the current programme. The candidate is preserved and **reopening requires a new owner decision**. This is a park rather than an unblocking, and it is deliberately not the same status as every other row in this section. **Reaffirmed 18 August 2026: parked, NOT blocked.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 18 now states the distinction explicitly. Nothing external prevents this work; it is a deprioritisation, so it must not be reported as an obstacle, counted among items awaiting a dependency, or picked up because a session mistook a park for a queue. |
| `INNOV-011` | Privacy-safe aggregate prediction distributions with a minimum cohort and low-count suppression | Contract 61's cohort rule | An aggregate read suppressing small cohorts | **Substantially implemented before promotion, and extended.** `get_prediction_consensus` (contract 61) and `get_season_prediction_consensus` (contract 130) already answer the per-fixture distribution behind a cohort of ten. Contract 176 adds the per-PLAYER half — agreement with the field and success against it — under the same cohort. What remains is scoreline-level distribution, which needs its own cohort decision because a scoreline cohort is far thinner than an outcome cohort |
| `INNOV-012` | Deterministic league side honours, each with a published window, denominator, tie behaviour, minimum sample and scope, altering no official points | An owner decision on each honour's definition | Honour functions plus pgTAP proving ties and empty states | Accepted — **not built.** Every honour is a product definition rather than an engineering one: "Form Player" over how many matchweeks, "Biggest Mover" over which table, and what a tie does. A migration inventing seven such definitions would be a product decision taken by a schema, which is the failure ADR 0027 § 8 and the `LMS-ORG` rows exist to prevent **Owner-decided 18 August 2026: each honour needs three things.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 26 requires every honour to state its WINDOW, its MINIMUM SAMPLE and its TIE rule before it may be implemented, and forbids an implementer choosing a missing one. The minimum sample is what protects the feature: an honour awarded off one matchweek is noise presented as achievement, and a league where nobody qualifies must show no honour rather than a weak one. |
| `INNOV-013` | Historical prediction exploration that never reconstructs rank movement it has no evidence for | Immutable historical evidence | A read, plus a recorded finding on whether the evidence exists | Accepted — **audited, and the evidence is sufficient without new storage.** Contract 150 derives rank movement over a settled matchweek from banked totals rather than a snapshot, and its own header records why a snapshot would be worse: it would add a second thing that can disagree with the first. `season_matchweek_scores` is immutable enough for the derivation because it is keyed on the round rather than on a date. **No snapshot table is required and none should be added** |
| `INNOV-014` | Retrospective closest-miss and counterfactual analysis, visually and structurally separate from real records | `INNOV-002`; for late-goal claims, reconciled provider event timing | Analytical reads, and a refusal to claim minute-level causality without event data | Accepted — **partially unblocked.** One-goal-away and alternative-scoreline analysis is derivable today from `season_predictions` and `season_fixtures`. **Minute-level claims are not**: this platform stores no event timeline at all, so "an 85th-minute goal cost you an exact score" cannot be said truthfully, and the candidate's own guardrail forbids saying it |
| `INNOV-015` | Optional prediction confidence, never affecting points, obeying the prediction lock, immutable after lock, on a bounded scale | An owner decision defining **what outcome is being calibrated** | A nullable bounded column, an atomic prediction-plus-confidence write, and pgTAP proving no scoring path reads it | **Owner-defined 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 13 defines it: confidence is the player's confidence that the **home/draw/away outcome implied by their score prediction** is correct — not confidence in the exact score. Optional, bounded **1 to 5**, locked and immutable on the same deadline as the prediction, and never changing points, rank or settlement. **Calibration reports must evaluate that defined outcome** and may not silently switch to exact-score accuracy. The product-definition blocker is therefore removed and the schema/read/write work can proceed. |
| `INNOV-016` | One server-side briefing read joining outstanding actions, the first deadline, today's fixtures and league context | Contract 162's action centre; contract 172's driver | A single read, or a measurement showing composition is adequate | Accepted — **not built, and the action authority must not be duplicated.** `get_my_actions` already answers the outstanding-action half from the only authority there is. A briefing read composing it with fixtures and league context is worth building **after** the action centre has a frontend consumer; building it now would ship a second uncalled read beside an uncalled one |
| `INNOV-017` | A cryptographic commitment to a submitted prediction set that cannot be altered after lock and can later be verified, without leaking low-entropy predictions through a guessable hash | A reviewed cryptographic construction | An immutable receipt record, a verification path, and tests proving a pre-lock receipt discloses nothing | Accepted — **not built, and the reason is recorded rather than deferred silently.** A scoreline has perhaps six bits of entropy, so a bare hash is brute-forceable in microseconds; a sound construction needs a server-held nonce or key, a key-rotation story and a review this session cannot supply. **What exists today already covers most of the trust claim**: `season_predictions` carries a server-set version advanced by a trigger, the lock trigger refuses every post-lock write including deletes, and contract 177 adds no path around either. The gap is *provable to a sceptic*, not *actually mutable* |
| `INNOV-018` | An independent read-only recalculation of settled scoring, comparing against banked totals, recording disagreements and correcting nothing | ADR 0012; settled matchweek totals | Independent implementation, mismatch storage, an administrator read, and a differential test proving the verifier can disagree | **Implemented — contract 178, and its report consumed 12 August 2026.** `run_shadow_scoring_verification` and `admin_shadow_scoring_report`, 18 assertions in `227_shadow_scoring_verifier.sql` including a deliberately wrong banked total the verifier names. `/admin/season` reads the report and offers **no control at all**, because the verifier corrects nothing and a repair button would claim an authority that exists nowhere. Two states that would otherwise look identical are kept apart: a season nothing has ever verified says so in those words and states that this is NOT a report that the scoring is right, and a clean run is printed with its bound including the verifier's own statement that it does not check the settlement lifecycle. **It still has no scheduled caller**, which is the same authority-with-no-caller shape contracts 162 to 172 record; scheduling it is a rollout decision and not a frontend one, and the page says so rather than inventing evidence |
| `INNOV-019` | Cross-provider and plausibility anomaly detection feeding the existing staged-proposal path rather than a second publication path | Contract 174's proposal model; more than one provider actually polling | Detectors plus tests, each producing typed evidence and no automatic truth | Accepted — **not built, and contract 174 is most of its foundation.** Detection, staging, naming rather than counting, and an administrator decision gate all exist. What is missing for the CROSS-PROVIDER half is a second provider: one provider is configured, so "three providers agree against one" has no data to run on. Single-provider plausibility detectors are buildable and were not attempted here rather than being shipped untested against real payloads **Owner-decided 18 August 2026: compare where coverage genuinely overlaps.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 24 directs two-or-more-source comparison wherever the sources actually cover the same fixture, and single-source plausibility detection everywhere else. Overlap is a measured property rather than an assumption. Everything continues to feed the existing staged-proposal path and its administrator decision gate; this authorises no second publication path, and a sentinel finding is never itself a result. |
| `INNOV-020` | Server-safe reconciliation of offline drafts: per-item outcomes, server-owned locks and versions, no client-supplied timestamp, explicit conflict reporting | Contract 47's version trigger; the season lock trigger | A batch write with per-fixture outcomes, and pgTAP proving partial acceptance and a reported conflict | **Implemented — contract 177, and the frontend half built 12 August 2026.** `save_season_predictions_batch`, 19 assertions in `226_offline_draft_reconciliation.sql`. Adds no rule and no second write path, proven by source assertion. The client half is a versioned local draft store scoped by schema, account, season and matchweek — validating the account against the PAYLOAD as well as the key, so a shared device cannot show one player another's work — and a reconciliation that honours every per-item outcome separately. Only `accepted` removes a draft; `locked`, `conflict`, `invalid` and `refused` keep it with the server's own sentence attached and stop it being re-sent, and a conflict puts both scorelines in front of the player without picking one. No state in the model means submitted, so no component can print one, and no client instant reaches the server |
| `INNOV-021` | Passkeys as an additional authentication method, preserving recovery and existing sign-in routes | Supabase Auth capability, measured rather than assumed | An integration through the supported flow, plus credential revocation and multi-device coverage | Accepted — **not built, and no bespoke implementation may be attempted.** The candidate's own guardrail requires the platform capability to be measured first, and this repository holds no auth-provider capability evidence. A hand-rolled WebAuthn credential store is explicitly out of scope: it is authentication cryptography, and getting it wrong is an account-takeover class of defect **Owner-decided 18 August 2026: pilot on native provider support.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 23 approves passkeys as an additive pilot on the authentication provider's native support, scoped to the Hub product initially, with existing sign-in and recovery routes intact. **The prohibition on a bespoke WebAuthn implementation is unchanged and absolute** — if native support does not cover a case, the answer is to stop, not to fill the gap locally. |
| `INNOV-022` | A private, revocable, regeneratable calendar subscription containing fixtures and deadlines and no prediction or private league data | An owner decision on token delivery, and a public HTTP surface to serve ICS | A token model hashed at rest, an Edge Function feed, and tests proving no prediction appears and a revoked token stops working | **Owner-approved 12 August 2026, unimplemented.** [ADR 0028](../adr/0028-owner-decisions-unblocking-product-work.md) § 16 approves a private, revocable and regeneratable calendar subscription. The token must be unguessable and stored safely — hashed at rest where practical — and revocation must stop future access. The feed carries **no prediction values and no private-league standings**, and its calendar/timezone behaviour follows the canonical fixture and deadline authorities. |
| `INNOV-023` | A bounded outstanding-action count from the same authority as the action centre | Contract 162 | A count that cannot disagree with the inbox it opens | **Already satisfied — no work required.** `get_my_actions` has returned `unseen` since contract 162, computed from the same predicate as the list it returns, and its own header records why. A second counting read is the duplicate authority this row exists to forbid |
| `INNOV-024` | Native-feeling view transitions | — | — | **No backend dependency.** Recorded so a later session does not re-audit it |

## Innovation Lab UI

Authority: the owner instruction of 11 August 2026 promoting the **UI-facing** portions of [`../product/innovation-lab.md`](../product/innovation-lab.md) into implementation, delivered as the UI pass recorded in that register. Twelve `INNOV-*` rows reached a production surface.

**This section holds only what the backend section above does not.** Both sessions ran at once and both wrote requirement rows; where a UI gap and a backend gap are the same requirement — `INNOV-003`, `-004`, `-005`, `-008`, `-009`, `-010`, `-015`, `-020`, `-021`, `-022` — the row above is the one row, and it is not repeated here. Two lists of one requirement is how a requirement gets implemented twice.

The four rows below are sub-requirements the UI pass discovered and the backend section does not carry. They take an `a` suffix rather than a new number, so the trail back to the Innovation Lab entry survives.

| ID | Requirement | Depends on | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| `INNOV-011a` | The cohort's predicted-goals distribution, so "your prediction was bolder than 86% of players" can be stated | A consensus read returning the distribution rather than the five most-predicted scorelines. Contract 176 added the per-PLAYER half; this is the per-COHORT half | The percentile derived from a full distribution and asserted against a seeded cohort, under the same minimum cohort | Accepted — unimplemented. The **outcome-share** half of `INNOV-011` is delivered in the UI; this is the half that was left out rather than approximated from five rows |
| `INNOV-012a` | Season-wide league honours — Comeback of the Season, a season Exact Score King, a season Form Player | A read spanning matchweeks for a league; deriving them in a browser is one request per matchweek. Each honour's window, denominator and tie behaviour is a product definition, per `INNOV-012` above | Each honour computed server-side with a published definition and a tie rule, asserted over a seeded season | Accepted — unimplemented. The **per-matchweek** honours are delivered in the UI and publish their own definitions |
| `INNOV-017a` | A server-supplied confirmation instant and receipt identifier for a submitted card — the ordinary half of `INNOV-017`, separable from the cryptographic commitment | `confirm_season_matchweek_card` returning them, and `get_season_matchweek_card` carrying them | A receipt quoting the server's own instant and identifier rather than the device's clock | Accepted — unimplemented. The shipped receipt **states no time and no reference**, and must never call an ordinary database write "verified". This row is deliberately narrower than `INNOV-017`: it needs no cryptographic review |
| `INNOV-019a` | A bounded administrator read over the staged provider **change** proposals contract 174 stages | The change-proposal evidence being browser-readable | An administrator seeing a staged change with its warnings and blockers named, and refusing to auto-confirm from the interface | Accepted — unimplemented. The staged **calendar** proposals and entrant inspection are already reachable; the change proposals are not. The cross-provider half is `INNOV-019` above and needs a second provider |

## Documentation safeguards

Authority: [`../ops/documentation-authorities.md`](../ops/documentation-authorities.md) § Safeguards for agent-readable documentation.

`DOC-AI-001`–`DOC-AI-010` are stated in that authority rather than duplicated here. They are rules about how this repository's documentation behaves, not product requirements, and they are enforced — where they are enforceable at all — by `scripts/check-documentation-authorities.mjs` and the tests under `tests/scripts/`.

## Supersession and history

This register was created on 6 August 2026 by the documentation reconciliation that also produced [ADR 0026](../adr/0026-public-site-separation-shared-accounts-and-euro-2028-acquisition.md). It supersedes no prior file. Before it existed, accepted-but-unimplemented requirements were spread across ADR prose, the roadmap and `MASTER-TODO.md` without stable identifiers, which is how several were lost and later rediscovered by audit.

The 10 August 2026 UI finalisation direction extends it again with `MIG-UI-01`–`MIG-UI-12`, for the same reason: those are the backend gaps a frontend session is most likely to discover, and a discovery recorded only in a session report is a discovery lost.

The 11 August 2026 Innovation Lab UI pass extends it once more with the `INNOV-*` rows above. They keep the Innovation Lab's own identifiers on purpose: a requirement that changes its name between the document that proposed it and the document that tracks it is a requirement two sessions will implement twice.

The 7 August 2026 Domestic Frontend Alpha reconciliation extends the same register rather than creating a second requirements file. Requirement rows remain here until implementation evidence exists; roadmap/TODO references may point to these identifiers without restating their acceptance evidence.
