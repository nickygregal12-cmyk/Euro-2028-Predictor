name: Reconcile acquisition risk once

on:
  push:
    branches:
      - docs/reconcile-acquisition-risk-2026-08-05
    paths:
      - scripts/reconcile-acquisition-risk-once.py
      - .github/workflows/reconcile-acquisition-risk-once.yml

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/reconcile-acquisition-risk-2026-08-05
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Reconcile acquisition-risk evidence
        run: python scripts/reconcile-acquisition-risk-once.py
      - name: Remove one-run tooling and commit
        run: |
          rm scripts/reconcile-acquisition-risk-once.py
          rm .github/workflows/reconcile-acquisition-risk-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/quality/acquisition-risk-register.md scripts/reconcile-acquisition-risk-once.py .github/workflows/reconcile-acquisition-risk-once.yml
          git commit -m "Docs: reconcile acquisition-risk evidence"
          git push origin HEAD:docs/reconcile-acquisition-risk-2026-08-05
