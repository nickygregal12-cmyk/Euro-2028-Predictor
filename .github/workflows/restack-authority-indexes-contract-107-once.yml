name: Restack authority indexes after Contract 107

on:
  push:
    branches:
      - docs/reconcile-authority-indexes-2026-08-05
    paths:
      - scripts/restack-authority-indexes-contract-107-once.py
      - .github/workflows/restack-authority-indexes-contract-107-once.yml

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/reconcile-authority-indexes-2026-08-05
          fetch-depth: 0
      - name: Merge current main
        run: |
          git fetch origin main
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git merge --no-edit origin/main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Refresh Contract 107 indexes
        run: python scripts/restack-authority-indexes-contract-107-once.py
      - name: Remove one-run tooling and commit
        run: |
          rm scripts/restack-authority-indexes-contract-107-once.py
          rm .github/workflows/restack-authority-indexes-contract-107-once.yml
          git add docs/design/README.md docs/competition-structure.md docs/architecture/multi-competition-hub-build-plan.md scripts/restack-authority-indexes-contract-107-once.py .github/workflows/restack-authority-indexes-contract-107-once.yml
          git commit -m "Docs: refresh authority indexes after Contract 107"
          git push origin HEAD:docs/reconcile-authority-indexes-2026-08-05
