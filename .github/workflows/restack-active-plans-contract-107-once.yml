name: Restack active plans after Contract 107

on:
  push:
    branches:
      - docs/reconcile-active-plans-2026-08-05
    paths:
      - scripts/restack-active-plans-contract-107-once.py
      - .github/workflows/restack-active-plans-contract-107-once.yml

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/reconcile-active-plans-2026-08-05
          fetch-depth: 0
      - name: Merge current main
        run: |
          git fetch origin main
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git merge --no-edit origin/main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Refresh Contract 107 boundaries
        run: python scripts/restack-active-plans-contract-107-once.py
      - name: Remove one-run tooling and commit
        run: |
          rm scripts/restack-active-plans-contract-107-once.py
          rm .github/workflows/restack-active-plans-contract-107-once.yml
          git add CLAUDE.md MASTER-TODO.md docs/roadmap.md scripts/restack-active-plans-contract-107-once.py .github/workflows/restack-active-plans-contract-107-once.yml
          git commit -m "Docs: refresh active plans after Contract 107"
          git push origin HEAD:docs/reconcile-active-plans-2026-08-05
