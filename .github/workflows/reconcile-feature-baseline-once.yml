name: Reconcile feature baseline once

on:
  push:
    branches:
      - docs/reconcile-feature-baseline-2026-08-05
    paths:
      - scripts/reconcile-feature-baseline-once.py
      - .github/workflows/reconcile-feature-baseline-once.yml

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/reconcile-feature-baseline-2026-08-05
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Reconcile feature baseline
        run: python scripts/reconcile-feature-baseline-once.py
      - name: Remove one-run tooling and commit
        run: |
          rm scripts/reconcile-feature-baseline-once.py
          rm .github/workflows/reconcile-feature-baseline-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/quality/feature-baseline.md scripts/reconcile-feature-baseline-once.py .github/workflows/reconcile-feature-baseline-once.yml
          git commit -m "Docs: reconcile feature and safeguard baseline"
          git push origin HEAD:docs/reconcile-feature-baseline-2026-08-05
