name: Reconcile active plans once

on:
  push:
    branches:
      - docs/reconcile-active-plans-2026-08-05
    paths:
      - scripts/reconcile-active-plans-once.py
      - .github/workflows/reconcile-active-plans-once.yml

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/reconcile-active-plans-2026-08-05
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Reconcile active planning documents
        run: python scripts/reconcile-active-plans-once.py
      - name: Remove one-run tooling and commit
        run: |
          rm scripts/reconcile-active-plans-once.py
          rm .github/workflows/reconcile-active-plans-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CLAUDE.md MASTER-TODO.md docs/roadmap.md scripts/reconcile-active-plans-once.py .github/workflows/reconcile-active-plans-once.yml
          git commit -m "Docs: reconcile active plans with delivered backend"
          git push origin HEAD:docs/reconcile-active-plans-2026-08-05
